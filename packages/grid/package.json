{
  "name": "@nanairo-sheet/grid",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Facade skeleton: Canvas 描画グリッドの唯一の公開面（mount/destroy stub）。実 API（Command/Event/Options・lifecycle 契約）は DD-016 で確定。内部パッケージを再エクスポートしない（境界文書 §3・R7）。ランタイム依存ゼロ。",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  }
}
