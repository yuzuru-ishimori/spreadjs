{
  "name": "@nanairo-sheet/sheet-formula",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "数式エンジン（tokenizer/parser/canonical AST/A1↔固定IDバインド/依存グラフ/評価器）。外部ランタイム依存ゼロ（ADR-022）・DOM/Node 非依存。セル値アクセスは CellReader 抽象。",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "typecheck:core": "tsc --noEmit -p tsconfig.core.json"
  },
  "devDependencies": {
    "@nanairo-sheet/sheet-types": "*"
  }
}
