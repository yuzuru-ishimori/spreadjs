設定・実装の主要部分は受け入れ基準に沿っていますが、ブランド型を相互に区別できるという重要な不変条件が現在のテストでは担保されていません。型が退化しても検証が成功するため、受け入れ基準上のテスト不足があります。

Review comment:

- [P2] ブランド型間の代入禁止を型テストで検証する — C:\repo\spreadjs\packages\sheet-types\src\ids.test.ts:29-35
  このテストは同じブランド型への正常な代入しか確認していないため、例えば `RowId` が誤って `DocumentId` と同じブランドになったり単なる `string` へ退化したりしても `npm run typecheck` は成功します。また、残り3種類のファクトリ戻り型も未検証です。識別子を取り違えられないことが §6.1 の主要な不変条件なので、異なるブランド間の代入に `@ts-expect-error` を置くなど、区別が失われた場合に型検査が失敗する負のテストを追加してください。