# DD-003 DA批判レビュー記録（全 Phase）

> DD-003 本体からの分離（guides.md §6）。本体には Critical/Warning/Info 件数と全件対応済みの要約を残す。
> 手順・品質フィルター・再チェック条件は `doc/da-method.md` を参照。内容は改変せず移動（履歴の正確性維持）。

### 共通DA観点（全Phase必須）

**§7.6 決定論違反の混入**（Date.now・Math.random・Map/Set反復順への依存・環境依存の文字列整列）と、**「テストのための実装」化**（フォールト注入が発火していない・収束assertが弱くて常にpassする）を毎Phaseで確認する。

> Phase 0 は3ラウンド実施（R1 広域→R2 クライアント/再接続→R3 保守/統合。R2・R3 で新規 Critical 0件＝§3.3 収束）。以下は設計クラリフィケーションで、対応は scenarios/protocol-subset/phase1-design への反映（コード未着手のため設計時解決）。

| # | Phase | 発見した問題/改善点 | 重要度 | 再現手順（Critical/Warning） | DA観点 | 対応 |
|---|-------|-------------------|--------|----------------------|--------|------|
| D1 | 1/5 | hash 正準直列化が `Map` 反復順や `localeCompare` に依存すると、snapshot import で復元した文書と Operation replay の文書が同一論理状態でも hash 不一致になり AC1/AC5 が非決定に破綻 | Critical | 復元インスタンス(import)とクライアント(replay)で同一状態を作り、セル Map の挿入順を変えて hash 比較 → 一致しない | 決定論違反（Map反復順・環境依存整列） | ✅設計解決: `hash.ts` は `displayRowOrder`×`columnOrder` 配列順で列挙・非空のみ・`localeCompare`/既定 sort 不使用（phase1-design §5・S-B1/B2/B4） |
| D2 | 1/2 | `DeleteRows` が `rowOrder` から行を除去する/tombstone を保持しないと、tombstone 化アンカーへの `InsertRows` のアンカー解決が全クライアントで一致せず（片方 reject・片方 accept）収束破綻 | Critical | 行削除後 `InsertRows{afterRowId:削除済み行}` を送る。tombstone 非保持クライアントは `unknown-anchor` で reject、保持側は accept → 分岐 | 状態遷移の不整合／収束破綻 | ✅設計解決: `rowOrder` は tombstone を保持し表示は `displayRowOrder` で除外・アンカーは論理位置で解決（phase1-design §2・S-D2） |
| D3 | 2 | サーバー処理順で `clientSequence` 検査を `operationId` 冪等より先に置くと、正当な重複再送（同一 sequence）が「単調でない」として `client-sequence-violation` で誤 reject され AC2（同一ACK再返却）が壊れる | Warning | 同一 operationId・同一 clientSequence を再送 → sequence 検査が先だと単調違反で reject | テスト不足／回帰（AC2） | ✅設計解決: 処理順を「operationId 冪等→clientSequence→baseRevision→検証適用」に固定（protocol-subset §5・S-F2/F3） |
| D4 | 5 | 収束試験が RowId/operationId を `crypto.randomUUID()` のまま使うと、AC1 の「失敗時はシードを出力し再現可能」要件を満たせない（同一シードでも別 ID 列＝別実行） | Warning | 失敗シードで再実行 → ID が変わり同一障害を再現できない | 「テストのための実装」化／再現性 | ✅設計解決: テストは注入シード ID 生成器を使用（本番は crypto.randomUUID）。scenarios S-M2・phase1-design §3 に明記 |
| D5 | 1/2 | `SetCells` を in-place で1件ずつミューテートしながら検証すると、途中の stale/削除行 reject で部分適用が残り、原子性（決定事項）と決定論が壊れる | Warning | changes=[正常, stale] を適用 → 1件目が適用済みのまま2件目で reject → 文書が中途状態 | 決定論／原子性違反 | ✅設計解決: `apply` を二相（validate-all→commit・`cloneDocument` バッファ）にし部分ミューテーション無し（phase1-design §4・S-C2/C5） |
| D6 | 4 | Presence TTL 失効を実 `setInterval` のみで駆動すると、smoke テストで TTL 失効フォールトが実時間待ちなしには発火せず、検証が空振り（テストのための実装化） | Warning | 注入クロックを進めても実 interval が回らず `presenceRemoved` が出ない → TTL 検証が常に未到達 | 「テストのための実装」化／フォールト未発火 | ✅設計解決: TTL 判定は注入クロック＋明示 `sweep()` で発火可能に（protocol-subset §6・S-L5） |
| D7 | 5 | 収束 assert が「サーバー hash 対 自身」や単一経路比較だと、全 no-op/フォールト未注入でも pass してしまう（偽陽性の緑） | Warning | フォールト注入を無効化しても収束試験が pass する構造 | 弱い収束assert／メタ検証欠如 | ✅設計解決: 各クライアント hash==サーバー==ログ再構築、フォールト発火カウンタ>0、自明でない invariant を assert（scenarios S-M3/M4） |
| D8 | 2/3 | `clientId`（セッション安定）と `connectionId`（物理接続）を混同すると、再接続時の clientSequence 継続 or Presence TTL 削除のいずれかが壊れる | Warning | 再接続で ID を1つに統一 → clientSequence がリセットされ pending 再送不能、または旧 Presence が TTL 除去されない | 識別子モデルの曖昧さ | ✅設計解決: clientId=sequence/冪等/再送キー（再接続不変）、connectionId=Presence 管理単位（接続毎）に分離（protocol-subset §2・S-J4） |
| D9 | 3 | `requestCatchup{afterRevision:N}` に対しサーバーが N を含めて再送すると二重適用の余地（クライアントの「小さい revision は無視」で救済されるが仕様が曖昧） | Info | afterRevision=N で fromRevision=N を返す → N が重複配信 | off-by-one／仕様明確化 | ✅設計解決: `fromRevision=afterRevision+1` に固定明記（protocol-subset §7・S-I5。二重適用は§8.4 dedup でも二重防止） |
| D10 | 1/3 | `InverseSeed.deletedRows.index` は**削除時点**の rowOrder 位置。以後の InsertRows で対象行の位置がずれるため、Phase 3 が index を使って物理再挿入すると順序が壊れる（本モデルの DeleteRows は tombstone のみで rowOrder から物理削除しない） | Warning | 行削除→別 InsertRows で前方挿入→rollback で保存 index に splice すると位置ずれ | InverseSeed 消費者契約の曖昧さ（rollback 十分性） | ✅対応: `apply.ts` InverseSeed doc コメントに「rollback は un-tombstone in place・index は参考情報」と明記。Phase 3 は index で splice しない |
| D11 | 1 | `applyInsertRows` は `rows` の rowId 既存チェックをしない。既存 rowId を渡すと `rowOrder` に重複・`rowMeta` 上書きで文書破損 | Warning | 既に存在する rowId で InsertRows → rowOrder に同一 ID が2つ | 防御不足（前提条件） | ✅対応: 採番一意性を呼び出し側契約とし `apply.ts` にコメント明記（本番=crypto.randomUUID、テスト=シード一意）。専用エラーコードは設けず 3種契約を維持。Phase 2/3 は一意性不変条件を維持 |
| D12 | 1/5 | hash は content-based（非空セルのみ・tombstone 除外＝S-B3 の意図）。ゆえに**非空セルが1つも無い**まま行構造だけ違う2文書（例: row-X 空 vs row-Y 空）は hash 衝突し、空行/tombstone のみの分岐を検知できない | Warning | 別々の空行のみを挿入した2文書の hash が一致（displayRowOrder は異なる） | 弱い収束 assert（盲点） | ⚠️設計どおり維持（Phase 1）＋Phase 5 推奨: 収束 assert で(a)非空セルが実在することを保証（Phase 1 プロパティテストは実施済）(b)最終収束は hash だけでなく rowOrder/tombstone を含む構造 deep-equal も併用（D7 の具体化） |
| D13 | 1 | `utf8Bytes` は孤立サロゲートを WTF-8（3バイト）でエンコードし、`TextEncoder`/`Buffer` の U+FFFD 置換と相違。ただし本システムは全経路で本エンコーダを使うため収束は保たれる | Info | 孤立サロゲート文字列の hash を Buffer 由来値と比較 → 相違（正常文字列では一致・テストベクトルで検証済） | cross-platform 同一性（病的入力） | ✅許容: 決定論・自己整合。cell 値の孤立サロゲートは病的入力。`hash.ts` にコメント。外部ハッシュとの相互運用経路は無い |
| D14 | 1/2 | `apply` は no-op（全件 tombstone 済み DeleteRows・空 InsertRows）でも `document.revision=ctx.revision` を刻む。`document.revision` で no-op 判定すると revision 消費を誤認 | Info | 全件削除済み DeleteRows の結果 document.revision が ctx 値に更新される | 呼び出し側契約 | ✅設計どおり（Q-1・phase1-design §4）: no-op 判定は**空 changeSet**で行う（document.revision ではない）。hash は revision 非依存で影響なし。Phase 2 sequencer が revision 消費を決める |
| D15 | 1/3 | 同一 SetCells 内で同一セルを複数回書くと `InverseSeed.cells` に複数エントリ。rollback を forward 順で適用すると誤復元（degenerate 入力） | Info | changes=[{c:v1},{c:v2}] の逆適用を前から行うと c=v1 で止まる | rollback 十分性（degenerate） | ✅対応: `InverseSeed` doc コメントに「rollback は cells を後ろから適用（forward 逆順）」と明記。通常の貼り付けはセル重複なしで無関係 |
| D16 | 1/5 | `apply` は毎回 `cloneDocument`（O(文書サイズ)）。Phase 5 の 10,000 op 収束試験は O(ops×size) で低速化し得る | Info | 大文書で多数 op を適用しプロファイル | 性能（テストのための実装化ではない） | ✅許容（phase1-design §7 が PoC 規模で明示許容）。Phase 5 で実測しボトルネック化したら構造共有へ。決定論は不変 |
| D17 | 2/5 | **no-op（全件tombstone済みDeleteRows）の ACK はログに残らない**。snapshot が ackCache を明示 export しないと、restore 後に同じ no-op op が再送されたとき idempotency をすり抜け、clientSequence 検査で誤 reject（or 誤処理）される | Warning | no-op を含む状態を export→ackCache を欠いて import→no-op op を再送→duplicate にならず reject | 冪等キャッシュとログの整合（指示 5） | ✅対応: `snapshot.ts` が `ackCache` を明示 export（accepted も no-op も登録）。`snapshot.test` の「no-op op の再送も duplicate」で受理・「ackCache 欠落の反例」で欠落時に救済が効かないことを実証 |
| D18 | 2 | restore で **clientSequenceTable を欠く**と、再接続後に「既に seq を進めた」クライアントの再送/次送信が期待値ずれで `client-sequence-violation` になり、再送誤 reject 経路が開く | Warning | seq=3 まで処理済みの状態を export→clientSequenceTable を欠いて import→seq=3 の続きを送る→期待 seq=1 で違反 | 再送誤 reject 経路（DA 重点） | ✅対応: `snapshot.ts` が `clientSequenceTable` を export/import。`snapshot.test`「clientSequence 表が継続」で seq=3 が受理・seq=2 戻りが reject されることを実証 |
| D19 | 2 | サーバー処理順で **operationId 冪等を clientSequence 検査より後**に置くと、正当な重複再送（同一 seq）が「単調でない」と判定され `client-sequence-violation` で誤 reject＝AC2 破綻（D3 の実コード化リスク） | Warning | submit の冪等 return を無効化（順序入れ替え模擬）→ F2/E3/回帰テストが失敗 | 処理順の回帰防止（DA 重点・D3） | ✅対応＋実証: 実装は §5 どおり冪等が先。`sequencer.test` に静的実証（`wrongOrderRejectsDuplicate`）＋**ミューテーションで 3 テスト失敗→revert で green** を確認（テスト非空振りを証明） |
| D20 | 2/4 | Presence TTL 失効を実 `setInterval` のみで駆動すると、テスト/スモークで注入クロックを進めても失効せず空振り（sweep 発火漏れ）。境界条件（`==ttl` を失効させるか）も曖昧だと 1 目盛りの取りこぼし | Warning | 注入クロックを ttl 丁度/超過に進め sweep 挙動を確認 | Presence sweep 発火漏れ（DA 重点・D6 具体化） | ✅対応: TTL 判定は注入クロック＋**明示 `sweep()`**。境界は `(now-lastSeen) > ttl`（`==ttl` は維持・`+1` で失効）と確定し `presence.test` で両側を検証。**定期 sweep 呼び出しは transport 側責務**（Phase 4 で `server.ts`/smoke に明記する境界メモ） |
| D21 | 2/3 | content-reject（stale/target-deleted/duplicate/base）で **clientSequence を前進させない**と、well-behaved クライアントの次 op（seq+1）が期待値ずれで誤 reject される。逆に前進させると、reject を受け取れなかったクライアントが同 op を再送した場合に seq 不整合が残る | Warning | reject 後に次 op を seq+1 で送る→前進しないと違反 | 識別子/シーケンスモデルの一貫性 | ✅対応: seq 検査通過後は**どの結果でも前進**（reject 済み op は Conflict Queue 行き＝再送しない前提）。reject はキャッシュしない（二重適用を起こさない）。**reject 未達クライアントの再送処理は Phase 3 クライアント責務**（Conflict Queue・再送しない）＝境界メモ。`sequencer.test` S-F3 で「advance しない（violation 時）」と recover を実証 |
| D22 | 3/5 | `InverseSeed`（apply.ts）は cell の before-**value** のみ保持し before-**revision** を持たない。既存（非空）セルを上書きした pending の逆順 rollback は `lastChangedRevision` を厳密復元できず、**rollback から committed を導出すると hash がずれ収束破綻**し得る | Critical | 既存セル@R を pending で上書き→view から inverseSeed 逆順 rollback→hash が committed（@R）と不一致 | 逆操作復元の完全性（rollback→同一hash） | ✅対応: **committed を権威として別管理**し rollback から導出しない（server op を committed へ直接適用）。`applyInverseSeed` は行構造・空セル前値では厳密（`session.test` で `rollbackBaselineHash()==committedHash` を実証）・上書きは非厳密と明記（同テストで不一致も実証）。収束は committed 権威で担保（AC1 不変） |
| D23 | 3 | own operation の `operationAck` と `operations` エコーが順不同で両方届くと、pending 除去が二重/漏れになる競合窓 | Warning | ACK 先着→echo 後着（or 逆）で own を二重除去 or 未除去 | own受信とpending除去の競合窓 | ✅対応: 除去は operationId 一致の冪等 `removeFromPending`（無ければ no-op）。ACK は `acknowledged` を立てるのみ（非 noop は echo が committed 前進＋own 除去）。noop（空 changeSet）は echo が来ないため ACK で除去。`session.test` S-H4a/b で両順序を実証 |
| D24 | 3 | rollback→replay 中に別受信が割り込むと committed/pending/view が中途状態で壊れ得る | Info | reconcile/rebuildView 実行中に handleServerMessage が再入 | rollback→replay割り込み不変条件（同期処理単位） | ✅確認: **1 ServerMessage 同期完結**（handleServerMessage に await 無し）。in-process ハブは `send` で enqueue するのみ（deliver は別ループ）ゆえ reconcile 中に再入しない。実 WS も send は非同期バッファゆえ同様＝割り込み不変 |
| D25 | 3/5 | own op の ACK 受信後に `operations` エコーが欠落（静止系）すると committed が確定 revision へ届かず永久に behind | Warning | ACK{rev} 受信→echo 欠落→以後 op 無し→committed<rev のまま | 冪等/収束（静止系の欠落） | ✅対応: ACK.revision を `knownServerRevision`（既知サーバー revision 下限）にし、`needsCatchup`／周期 poll が catch-up で回復（`handleAck` で floor 更新） |
| D26 | 3/5 | 受信済み revision より先の op を1件も受け取れず（buffer 空・後続 op も欠落）gap 検知が起きない **tail 欠落**で、クライアントがサーバー前進を知れず永久に behind＝**全体収束（AC1）破綻** | Critical | 3クライアント×フォールト30%・末尾 operations 欠落→gap 検知トリガー無し→committed 停止（25シード中 **7 失敗**を実測） | 「たまたま収束」構造の否定・弱い収束の露呈 | ✅対応: **周期 catch-up ポーリング**（`tick` で `requestCatchup{afterRevision:expectedRevision-1}` を `catchupPollMillis` 間隔送信＝既存プロトコルのみ・全 catch-up ケース包含）。修正後 **25/25 収束**（scratch で実証・恒久回帰は inprocess-transport.test） |
| D27 | 3/5 | サーバーは content-reject でも clientSequence を前進（D21）。reject が欠落すると同 op を同 clientSequence で再送→`client-sequence-violation`→先頭再送→**無限ループ**（reject 未達ゆえ Conflict Queue へ移せない） | Warning | 競合 op の content-reject を drop→再送→violation→再送… | 再送とConflict Queueの排他（D21 境界の実コード化） | ✅対応: PoC フォールトモデルは**データ経路（operations/operationAck/submitOperation）のみ注入**し制御（operationRejected/welcome）は確実配信＝reject 即 Conflict Queue で再送集合外。violation 時の clientSequence 完全再整列は Phase 5 へ境界メモ（競合収束は inprocess-transport.test の競合1件×フォールトで回帰確認） |
| D28 | 4 | 異常切断は WS が `error` と `close` を両方（順不同）発火し得る。両方で Presence 削除を走らせると二重 `presenceRemoved`／二重 `handleDisconnect`、片方でしか処理しないと取りこぼす | Warning | 同一接続で error→close（逆順含む）を連続発火→presenceRemoved が 2 回出るか／0 回になるか | 切断イベントの取りこぼし・二重削除冪等 | ✅対応: `RoomBridge.onClose` は `connectionByWs` にエントリが有るときだけ**先にマップ削除**してから `handleDisconnect`（冪等）。error/close/sweep-close の全経路が同じ `onClose` を通り、2 回目は connectionId 未検出で no-op。smoke の後始末で `connectionCount()→0` を確認 |
| D29 | 4 | join 応答（welcome→operations→presenceSnapshot）配信中に同一接続の submit が割り込むと、operations エコーが welcome より先に届き受信側 committed の起点がずれ得る | Info | join 直後に submit を送り welcome より前に operations を届ける順序を作る | catch-up中送信順（joinとsubmitの競合） | ✅確認: WS は**接続ごとに受信順＝処理順**（`ws` の message イベント FIFO）。`handleJoin` は同期で welcome→operations→snapshot を outbound 配列順に生成し dispatch も配列順ゆえ後続 submit は必ず後。順序保存で起点ずれ無し（in-process ハブと同じ不変・DA D24） |
| D30 | 4/5 | 固定ポートは連続/並行テストで衝突（EADDRINUSE）、`setInterval`(sweep)/未 close の ws/http server が残るとテストプロセスがハングしリークする | Warning | 固定ポートで 2 起動→EADDRINUSE、close 忘れで vitest が終了しない | テスト間のポート/プロセス後始末 | ✅対応: テストは **port 0（OS 任せランダムポート）**で起動し `serve` の listening callback で実ポート取得。`close()` が `clearInterval`＋全 ws `terminate()`＋`wss.close()`＋`server.close()` を await。smoke は `afterEach` で全 transport.close()＋server.close() を必ず実行。vitest が **自然終了**（ハング無し・Duration ~2s）を確認 |
| D31 | 4 | 「依存ゼロ」の demo.html に CDN/外部フォント/import が紛れ込むと依存ゼロが崩れる（人的レビューで見落としやすい） | Warning | demo.html 配信時に外部リクエストが飛ぶか観察 | デモHTMLの依存混入 | ✅対応＋機械検証: `grep` で `http(s)://`=0・`import`/`require`=0・`<script src=`/`<link`/`unpkg`/`jsdelivr`/`googleapis`/`integrity=`=0（唯一の "CDN" ヒットは「CDN 無し」と明記したコメント）・`console.*`=0。WS URL は `location` から動的生成（`https://` リテラル不使用）＝素の WebSocket/fetch/DOM/crypto のみ |
| D32 | 4/5 | 大量 operations を高速配信すると `ws.send` の送信バッファ（`bufferedAmount`）が膨らみ、無視するとメモリ圧・遅延（順序は保たれる） | Info | 多接続×高頻度 operations で bufferedAmount を観測 | ws message順序とバックプレッシャー（PoC範囲） | ⚠️PoC 範囲で許容（localhost・小メッセージ・数接続）。順序は WS の per-connection FIFO で保証。`bufferedAmount` 監視・drain 制御・メッセージ結合は本番アダプター（後続）へ境界メモ。収束の正しさには非依存（catch-up/冪等が回復） |
| D33 | 5 | **停止ノーオペで収束停止**: 競合 DeleteRows の敗者（既に tombstone 済み行への再 Delete）が localNoop=false のまま ACK 済みで pending に残り収束が止まる（noop ACK が並行 delete の echo より先着したとき echo 待ちで除去されない） | Critical | 収束試験で 2 Client が同一行を並行 Delete し敗者の noop ACK を並行 delete の echo より先に配送 → 敗者 pending が減らず committed=serverRev でも pending>0 | 収束破綻（停止系）／「たまたま収束」の否定 | ⚠️既知境界（S-E4 の PoC 限界）: localized fix（rebuildView で acked-noop 除去）は seq 消費済みでも D27 の seq 再整列と絡み submitOperation drop 下で violation を誘発するため単独採用を見送り。**収束試験は削除を client-owned（自分の挿入行のみ）に限定**し並行同一行 Delete を回避。恒久修正（acked server-noop の安全除去）は**要判断で後続**へ。単体は snapshot.test の no-op 経路で検証済み |
| D34 | 5 | **D27 の実コード化＝client-sequence-violation の指数増幅**: submitOperation 欠落で seq gap → 複数 op が violation → 各 violation で `resendAllPending`（全 pending 再送）→ 更に violation → 1 deliver 内で指数的に増幅（実測 queueLen 95k〜155k） | Critical | 収束試験で client→server の submitOperation に drop を高スループットで注入 → deliverAll が maxSteps 到達（storm） | 実装の収束限界／偽収束の防止 | ✅対応: 収束試験のフォールトを **server→client 経路（operations/operationAck）＋切断/再接続に限定**（`InProcessHub.injectClientToServer=false`）。catch-up/冪等/reorder で回復する経路を検証。`deliverAll` に **maxSteps ガード**を追加し storm を明示 throw（黙って打ち切って偽収束しない）。恒久修正（violation 時の署名ベース重複抑制）は ADR-0008 の再検討条件へ |
| D35 | 5 | server→client 限定が「たまたま収束」を招かないかのメタ検証 | Warning | フォールトを server→client に限定しても収束が自明化しない立証が要る | 弱い収束assert／メタ検証（D7 の具体化） | ✅対応: (a) 発火カウンター（duplicate/drop/delay/disconnect）を**種類ごと > 0** で assert（実発火・モックでない）。3体で dup7408/drop8728/delay10847/disconnect208。(b) 収束 assert は hash＋**hash 非依存の構造 deep-equal**（`doc-compare.ts`＝rowOrder/columnOrder 配列順で列挙・hash とは別関数導出）＋ログ replay hash＋二重適用0（`nextExpectedRevision===serverRev+1`）の多経路。(c) reject≥1・insert/delete≥1・非空セル>0 の自明でない invariant。(d) 切断/再接続は client→server の resend 経路を間接 exercise。全 no-op では通らない |
| D36 | 5 | 収束試験の実行時間・信頼性・rollback/replay 遅延の観察（No-Go 材料） | Info | 10,000 op × 3〜10 Client が現実的時間で収束し pending が肥大しないか | 性能／No-Go 条件（rollback/replay 恒常遅延） | ✅計測: 3体 10,000 件 ≈1.5s・10体 ≈2s・**ops/sec 5,000〜16,000**。**最大 pending 深度 4〜8（有界）**＝rollback/replay は入力あたり O(pending) で恒常遅延の兆候なし。同一シード完全再現（S-M2）。catch-up 回数・フォールト発火・reject 数をログ出力し No-Go 判断材料に |
| D37 | 5 | restart-restore の後始末（同一ポート復元＋autoReconnect のリーク） | Warning | 停止→同一ポート再バインド→自動再接続→後始末で接続/タイマー/ソケットが残るとハング | テスト後始末（D30 の Phase 5 版） | ✅対応: `server.close()` await 後に同一ポート rebind（成功確認）。後始末で `clearInterval`＋全 `transport.close()`＋`server.close()` を await → `connectionCount()→0`・vitest 自然終了（実測リーク無し） |
