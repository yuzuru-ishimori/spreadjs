# DD-012-2: 性能縦切り

| 作成日 | 更新日 | ステータス | 補足 |
|--------|--------|-----------|------|
| 2026-07-13 | 2026-07-13 | 確認待ち | 親=DD-012（案Y分割）。CG-6 担当。Phase 0/1判断＋Phase 2/3計測足場完了・**headed実測待ち**（人手）。render抽出は DD-016 委譲（要判断・DD-012-1と一貫） |

```text
Risk Class: A
Risk Triggers: パフォーマンス特性が変わりうる変更（描画基盤の抽出・Canvas描画キャッシュ経路）／自動試験で判定不能な受け入れ条件（headed 実測・精密メモリ計測＝CG-6）／未解除CG（CG-6）は変更トリガー方式の例外（roadmap §0・§2.4）
Human Spec Gate: required → 充足済（DD-012 仕様確認ゲート 2026-07-13: 性能合格ライン=DD-004実測の予算化。spec/AC変更が生じたら再ゲート）
Codex: high（挙動保存の抽出＋計測ゲート整備。状態機械・protocol・永続化アルゴリズムの実質変更なし＝xhigh 非該当・roadmap §2.2 L3）
Manual Gate: あり＝5万行×200列 scroll/selection の headed 実測＋CG-6 精密メモリ計測（`performance.memory` 封鎖回避・pocd-browser-bench 流用）
External Review: 不要（Phase境界・API確定・ADR転換・Go に非該当）
Evidence Level: full（A区分・CG-6 解除証拠。計測環境・再現コマンド・生ログ参照を省略しない。格納先 `doc/DD/DD-012-2/`）
```

> アプローチ: 標準＋実測駆動（挙動保存の抽出＋DD-004/006 計測ハーネス流用の headed 実測が中心）

## 目的

利用者成果=「**5万行×200列で scroll/selection が快適（回帰予算内）**」を担保する。PoC-B 描画資産を `@nanairo-sheet/render` へ実抽出（挙動保存）し、§23 Phase 1完了条件「5万行の基本 scroll/selection」を**統合性能回帰ゲート**（合格ライン=DD-004 実測の予算化）として常設化（`tests/invariants/perf`）し、**CG-6（精密メモリ計測）を解除**する。

## 背景・課題

- DD-009 資産台帳の割当: `pocb/{base-layer,overlay-layer,viewport,scroll-anchor,dpi,axis,text-cache,render-scheduler}.ts`＝Harden（→`@nanairo-sheet/render`）。`document-view.ts` 結線（DD-012-1 で grid 側に位置づけ済）を維持。
- **CG-6**: `performance.memory` 封鎖を回避した精密メモリ計測が未実施。期限=Alpha exit前・未解除は「データ上限明示 or Alpha不可」。担当=本DD（roadmap §0）。
- 計測ハーネスの再判断（台帳 Q2/Q3/Q4）: `apps/pocd-bench`（CLI 計測）・`apps/pocd-browser-bench`（500k セル browser 実測・`performance.memory`）・`integration/initial-load-metrics.ts`／`pocb/metrics.ts` は流用を第一候補とし、Phase 2 冒頭で流用/作り直しを判断。
- DD-011 設置の `tests/invariants/perf` は暫定予算の最小スモークのみ → 本DDで実予算（DD-004 実測基準）へ差し替え・常設化。
- boundary baseline のうち `pocb` 由来の R1 entries（owner=DD-012 分）を本DDで縮退。
- **依存: DD-012-1 完了後**（入力経路・selection 抽出が確定した状態の統合ページで計測する。描画抽出は挙動保存のため DD-012-1 の CG-1 実機証拠を無効化しない前提。挙動差が出た場合は実機スモーク再判定を親DDへエスカレーション）。

## 検討内容

- **合格ライン（ユーザー確定③）**: DD-004 実機実測を回帰予算の基準にする — **scroll p95 16.8ms・選択 16.9ms・再描画 0.33ms・メモリ 300MB内**（DD-006 実測整合）。計測ノイズ許容（マージン）の与え方は Phase 2 の実装前詳細化で予算表として固定し、DD本文に記録する（予算を緩める変更は spec 変更＝再ゲート対象）。
- **計測条件**: 5万行×200列（A-03 アドレス可能範囲）。セル充填率・列幅・行高は DD-004 計測条件を踏襲し、差分があれば予算表に明記。
- **CG-6 の計測方式**: `performance.memory` は既定で precision が封鎖されるため、精密値の取得方法（Chromium 起動フラグ等）＋`pocd-browser-bench` の流用可否を Phase 3 冒頭で確定。単発値でなく時系列（リーク傾向）も記録（DD-004 の手法踏襲）。

## 性能予算表（Phase 2 で固定・ユーザー確定 2026-07-13）

> 正典 = `scripts/cg-perf/perf-budget.json`（機械可読 SSOT）。合格ライン＝DD-004『実機確認run』（2026-07-12・実Chrome）実測。**予算を緩める変更は spec 変更＝再ゲート**（tripwire: `tests/invariants/perf/perf-judge.test.ts` が値をピン留め）。

| メトリクス | 回帰予算（合格ライン） | §18.2 機能上限（floor） | ノイズマージン |
|---|---|---|---|
| scroll frame p95 | ≤ 16.8ms | < 33ms | +20%（timing・暫定★） |
| 選択応答 worst | ≤ 16.9ms | < 50ms | +20%（timing・暫定★） |
| 停止中 full 再描画 mean | ≤ 0.33ms | ≤ 12ms | +20%（timing・暫定★） |
| メモリ ピーク | ≤ 300MB | — | 0%（DD-004 実測 ~29MB＝約10倍余裕） |
| メモリ リーク傾向 | slope < 64KB/s AND growthRatio < 1.25 | — | — |

- **計測条件**: 50,000 行 × 200 列・非空 500,000（seed=20260712・uniform-sparse）・セル基準 56×22px・可視セル 2,000〜4,000 帯（帯外＝負荷条件未達＝合格根拠にしない）。DD-004 measurement-spec 踏襲。
- **判定 3 値**: `pass`（予算内）／`over-budget`（予算超・機能上限内＝回帰の疑い＝再ゲート）／`fail`（機能上限超）。
- **★ノイズマージン 20%** は DD-004 自身の run 間分散（実機確認run 0.33ms ↔ MCP run 0.39ms ≈18%）吸収のための暫定値。headed ゲートでユーザー確定（緩める＝spec 変更）。

## 決定事項

- （render 抽出は DD-016 委譲。上記 AC 注記参照）抽出時は**挙動保存**（描画結果・キャッシュ挙動・DPI 挙動を変えない）＋既存テスト green 維持。規約・設計の新規発明をしない。
- 性能予算は `tests/invariants/perf` に**常設回帰ゲート**として固定し、以後のDD（DD-013〜016）が §2.3 L4 の発動条件（描画キャッシュ・replay 方式等の変更）でフル再計測する基盤にする。
- 5万行 headed 実測・メモリ計測は再現コマンド付きでレポート化（`doc/DD/DD-012-2/`）。
- 新 workspace（render）追加は package-lock 変更を伴う → 着手前に `git status`＋並行セッション確認。

## 受け入れ基準

> **スコープ調整（実装判断 2026-07-13・要判断で親DDへ返却）**: `@nanairo-sheet/render` の**物理抽出と baseline 縮退は DD-016（grid Facade 配線時）へ委譲**する。理由=今抽出すると apps/playground(integration)→render の R1 が +5・pocb 由来解消 −2＝正味 baseline 肥大（AC5 と逆行）で、かつ render が chunk-store/presence-sim/selection（未 package 化）へ型依存＝render→apps の R4 になり、grid Facade＋selection package 化（DD-016）なしにクリーン抽出不可。DD-012-1（ime/selection 抽出の DD-016 委譲）と一貫。詳細=`DD-012-2/evidence.md §2`。**AC1/AC5 の「実抽出・縮退」該当部は DD-016 委譲**（下表注記）。本DDは new=0（baseline 不変）を維持し、性能/メモリゲートの常設化＋headed 実測足場に集中する。

| # | 基準（操作 → 期待結果） | 検証方法 |
|---|------------------------|---------|
| 1 | ~~`@nanairo-sheet/render` 抽出後、回帰なし~~ → **DD-016 委譲**（render 抽出前提）。本DDは pocb 現位置維持で `npm run test`/`typecheck`/`lint`/`build` green＝回帰なしを担保 | Phase 1 🔬（抽出=DD-016） |
| 2 | **5万行×200列 headed 実測**: scroll p95 ≤ 16.8ms・選択応答 ≤ 16.9ms・再描画 ≤ 0.33ms（予算表=Phase 2 で固定・DD-004 実測基準）を満たす。再現コマンド付きレポート | Phase 2 headed 🔬 |
| 3 | 性能予算が `tests/invariants/perf` に常設化され `npm run test:invariants` green（暫定予算を実予算へ差し替え） | Phase 2 🔬 |
| 4 | **CG-6 解除証拠**: `performance.memory` 封鎖を回避した精密メモリ計測で5万行文書の実測（時系列含む）がメモリ 300MB内。レポートを `doc/DD/DD-012-2/` へ格納・CG台帳更新 | Phase 3 headed＋証跡 |
| 5 | ~~baseline の担当 R1 entries（pocb 由来）縮退~~ → **縮退は DD-016 委譲**（render 抽出前提）。本DDは**新規違反0（baseline 不変）**を維持 | Phase 1 🔬（縮退=DD-016・new=0=本DD） |

## タスク一覧

### Phase 0: 事前精査
- [x] 📋 各Phaseのタスク精査・詳細化（AC対応・ファイルパス・🔬タスクの有無）
- [x] **DD-012-1 完了確認**（アーカイブ済＝`doc/archived/DD/DD-012-1`・ヘッダ完了・CG-1 解除済）を確認
- [x] 📐 実装前詳細化トリガー判定: 規模・複雑度（性能特性）該当。判定: `Phase 1〜2 → 詳細化要／Phase 3 → 不要`
- [x] 🧑‍⚖️ Codexレビュー要否判定: `必須・effort: high`（Phase 4 で全差分一括1回）。Codex 利用可（2026-07-13 `--check` exit 0）
- [x] 😈 Devil's Advocate調査 → 下記ログ＋evidence へ反映（抽出で CG-1 証拠無効化しないか＝抽出は DD-016 委譲で回避／計測が PoC 再演で製品経路を測れているか＝poc-b 計測ページの限界を手順書に明記／予算マージンが緩すぎないか＝§18.2 機能上限を floor に二段判定＋20% は暫定★）
- [x] 着手前確認: `git status`（working tree clean）＋並行セッション確認（新 workspace 追加は render 抽出見送りで発生せず＝package-lock 変更なし）

### Phase 1: render 抽出（→ **DD-016 委譲**・要判断）
- [x] 📐 実装前詳細化 → **要判断: render 物理抽出は DD-016 委譲**（baseline 肥大＋render→apps R4・grid Facade/selection package 化が前提）。根拠=`evidence.md §2`・下記ログ
- [ ] ~~`packages/render/` へ 8 資産移設~~ **DD-016 委譲**: apps(integration)→render R1 +5・pocb 由来 −2＝正味肥大、かつ chunk-store/presence-sim/selection への型依存で render→apps R4。grid Facade＋selection package 化（DD-016）が前提
- [x] `apps/playground` の import 追随は抽出見送りにより不要（pocb は現位置維持）／`pocb/main.ts`・`harness.ts` は計測ドライバーとして残置（Phase 2 で流用）
- [x] `scripts/boundary/baseline.json`: 抽出見送りのため **new=0 を維持**（baseline 不変・縮退は DD-016 依存＝evidence.md 記載）
- [x] 🔬 **機械検証**: `npm run test`・`typecheck`・`lint`（new=0）・`build` green（回帰なし＝現位置維持で担保。統合ページ描画の目視スモークは人手）
- [x] 😈 DA批判レビュー（抽出しないため描画キャッシュ・DPI・scroll anchor に挙動差なし＝CG-1 証拠を無効化しない）

### Phase 2: 5万行統合性能回帰ゲート（予算=DD-004実測）
- [x] 計測ハーネスの流用判断（Q2/Q4）→ **流用採用**: `pocb/{harness,metrics,main}` を計測に流用・判定器は package 外 harness（`scripts/cg-perf/`）。`pocd-bench`(CLI)・`initial-load-metrics` は scroll/selection の fps を測れず非採用（初期ロード別）。ログ記載
- [x] 📐 実装前詳細化: **予算表を固定**（scroll p95 16.8ms・選択 16.9ms・再描画 0.33ms・計測条件・ノイズマージン20%★）→ DD本文「性能予算表」＋`scripts/cg-perf/perf-budget.json`
- [ ] 5万行×200列 **headed 実測**（scroll/selection/再描画）→ **人手セッション待ち**（手順=`perf-realmachine-procedure.md`・judge=`scripts/cg-perf/judge-perf-report.mjs`）
- [x] `tests/invariants/perf` の常設化: 実予算を `perf-budget.json`＋判定器＋`perf-judge.test.ts`（fixtures 検証＋予算ピン tripwire）で固定。§2.3 L4 発動条件は `tests/invariants/README.md` 記載済
- [x] 🔬 **機械検証（自動分）**: `npm run test:invariants` green（AC3・判定器＋予算ピン）。headed 実測が予算内（AC2）は人手待ち
- [x] 😈 DA批判レビュー（warm/cold・p95 標本数・可視セル帯の観点を `perf-realmachine-procedure.md` 手順＋負荷条件ゲートに反映）

### Phase 3: CG-6 精密メモリ計測
- [x] `pocd-browser-bench`（Q3）流用可否＋`performance.memory` 封鎖回避方法を確定し手順文書化 → `cg6-memory-procedure.md`（第一候補=`--enable-precise-memory-info`・相互チェック=`measureUserAgentSpecificMemory`）
- [ ] 5万行文書の精密メモリ実測（単発値＋時系列）→ 300MB内確認・レポート格納（AC4）→ **人手セッション待ち**（judge=`judgeMemoryReport`）
- [ ] CG台帳 CG-6 解除更新（未達なら「データ上限明示」を親DD経由でユーザー提示）→ **headed 実測後**
- [x] 🔬 **機械検証**: 計測スクリプトの再現実行（`node scripts/cg-perf/judge-perf-report.mjs <fixture>`）で判定が再現＝`perf-judge.test.ts` で常設検証
- [x] 😈 DA批判レビュー（GC タイミングの記録要求を `cg6-memory-procedure.md` 手順へ明記）

### Phase 4: Codexレビュー（high・全差分一括）＋証跡
- [ ] Codexレビュー自動実行（依頼書 `doc/DD/DD-012-2/codex-review-request.md` → `bash scripts/codex-review.sh` → `doc/DD/DD-012-2/codex-review-result.md`・effort high）
- [ ] Codexレビュー指摘への対応、または見送り理由をログに記録
- [ ] `doc/DD/DD-012-2/evidence.md`: full 証跡（AC対応表・計測環境・再現コマンド・生ログ参照・既知の未保証境界）＋密度計測（§2.4）記録
- [ ] 🔬 **機械検証**: 最終一括 green（`npm run test`・`typecheck`・`lint`・`build`・`test:invariants`）＋`bash scripts/doc-check.sh` green
- [ ] 😈 DA批判レビュー（証跡から第三者が計測を再現できるか＝Evidence full 監査）

## ログ

### 2026-07-13
- DD作成（親 DD-012 の案Y分割確定〔2026-07-13 ユーザー〕を受けて子DDとして起票。旧 DD-012 Phase 3・4 相当を移管。dd-drafter）
- Codex 利用可否: **利用可**（`bash scripts/codex-review.sh --check` exit 0・codex-cli 0.144.0-alpha.4）
- 確定済の前提: 性能合格ライン=DD-004 実測の予算化（scroll p95 16.8ms・選択 16.9ms・再描画 0.33ms・メモリ 300MB内）／依存=DD-012-1 完了後
- 要確認: なし（予算マージン・計測条件の詳細は Phase 2 の予算表で固定し、予算を緩める場合のみ再ゲート）
- Playwright MCP: headed 実測 Phase で使用予定（未導入なら手動実測で代替し、その旨をログへ記載）

### 2026-07-13（実装セッション・Opus / dd-implementer）
- **着手前**: `git status` clean・DD-012-1 アーカイブ済（`doc/archived/DD/DD-012-1`）を確認。並行セッションの新規物との重複なし。
- **Phase 1 要判断（render 抽出 → DD-016 委譲）**: 8 資産を今 `packages/render/` へ抽出すると、統合ページ `integration/{document-view,main,editor-placement,ime-editing-session,integration-editor}.ts` → `@nanairo-sheet/render` の R1 が **+5**、pocb 由来（scroll-anchor/viewport → types）の解消は **−2** で**正味 baseline 肥大**（AC5 と逆行）。さらに `base-layer`→`type ChunkStore`（chunk-store 非 package）・`overlay-layer`→`type PresenceUser`/`type CellRange`（presence-sim/selection 非 package・selection は DD-012-1 で DD-016 委譲）で render→apps の **R4** となり、**grid Facade 配線＋selection package 化（=DD-016）なしにクリーン抽出不可**。DD-012-1（ime/selection 抽出の DD-016 委譲）と同型・一貫と判断し **DD-016 委譲**。部分 Facade 新設・DAG 拡張＝規約の新規発明はしない。AC1/AC5 の「実抽出・縮退」を DD-016 委譲へ調整、new=0（baseline 不変）維持。根拠=`evidence.md §2`。
- **Phase 2 予算固定＋常設化**: 予算表（scroll p95 16.8 / 選択 16.9 / 再描画 0.33ms / メモリ 300MB・計測条件 5万×200・可視セル帯・ノイズマージン timing 20%★暫定）を DD本文＋機械可読 SSOT `scripts/cg-perf/perf-budget.json` に固定。判定器 `scripts/cg-perf/perf-judge-core.mjs`（3 値 pass/over-budget/fail＋負荷条件ゲート）＋CLI `judge-perf-report.mjs`＋fixtures 4 種。常設テスト `tests/invariants/perf/perf-judge.test.ts`（判定器機械検証＋予算ピン tripwire）。§18.2 機能上限を floor に二段判定（緩すぎる素通し防止＝DA 反映）。計測ハーネスは pocb 流用（Q2/Q4）。
- **Phase 3 CG-6 封鎖回避**: `cg6-memory-procedure.md` に `--enable-precise-memory-info`（第一候補）／`measureUserAgentSpecificMemory`（相互チェック）／CDP（監査）を記載。`judgeMemoryReport`（ピーク ≤300MB AND slope<64KB/s AND growthRatio<1.25）。GC タイミング記録を手順へ明記（DA 反映）。
- **headed 実測足場**: `perf-realmachine-procedure.md`（Phase 2）・`cg6-memory-procedure.md`（Phase 3）で人手セッションの再現コマンドを整備（CG-1 の `scripts/cg1/` と同型）。実測値の取得だけ人手に残す（AC2/AC4）。
- **機械検証（自動分）**: `npm run test` / `typecheck` / `lint`（boundary new=0） / `build` / `test:invariants` の結果は下記「機械検証結果」節。
- **Codex（Phase 4・high・1回）**: 依頼書 `codex-review-request.md` → 結果 `codex-review-result.md`。findings **8件（P1×2・P2×6）＝全件妥当・全件対応（見送り0）**:
  - [P1] 未計測を pass にしない: `judgePerfReport` を「3 メトリクスすべてに標本があり、かつ `frame.count>0`」でのみ pass に（`rollup` 修正・`perf-report-partial.json` で検証）。
  - [P1] CG-6 誤解除防止: `leakTrend.minSamples 2→8`＋`minDurationSec 90` を追加し、`judgeMemoryReport` に計測時間下限を導入（10 秒平坦標本を n/a に。`memory-short.json` で検証。DD-004 実測=約100秒/11標本に整合）。
  - [P2] 可視セル未取得（0/欠落）も帯外と同様 `n/a`（`conditionUnmet = !inBand`）。
  - [P2] `growthRatio` 欠落時はリーク判定を通さない（slope・growth 両方の有限値を必須）。
  - [P2] scroll/selection の機能上限をちょうど 33/50ms でも fail（strict 比較）。再描画は `≤12ms`（非 strict）。`perf-judge.test.ts` に境界テスト追加。
  - [P2] tripwire を受け入れ条件全体へ拡張（`budget`＋`hardCeiling`＋`noiseMargin`＋`visibleCellBand`＋`rows/cols`＋`leakTrend` をピン留め）。
  - [P2] `pocd-browser-bench` は単発値のみで judge 非互換 → 時系列は pocb ハーネスで採取・pocd-browser-bench は桁確認の相互チェックに限定と手順書を修正。
  - [P2] DD ステータス更新に伴い `bash scripts/dd-index-gen.sh` で DD-INDEX 再生成。

### 機械検証結果（2026-07-13）
- `npm run lint`（eslint＋boundary）: **green**（boundary `baselined=41 new=0 stale=0`＝新規違反0・baseline 不変）。
- `npm run typecheck`: **green**（全 workspace）。
- `npm run test:invariants`: **green**（27 tests・うち perf-judge 9 tests＝判定器＋予算ピン）。
- `npm run test`（全体）: **632/633 pass**。唯一の失敗＝`ws-convergence.smoke`（既知 flaky・環境依存の WS waitFor timeout・本DDは collab/server コード不変＝非回帰）。`cell-store-differential` は明示 timeout 内で pass。
- `npm run build`: **pre-existing 環境要因で fail**（vite 6.4.3 の `html-inline-proxy` が `index.html` のインライン `<style>` を Windows パス（`C:/...` 混在スラッシュ）で解決できない既知不具合）。**本DDの変更を stash した clean tree でも同一 fail を再現＝DD-012-2 の回帰ではない**。本DDは playground/HTML/CSS/vite 設定を一切変更していない。要フォロー（別途）。
- 判定器再現: `node scripts/cg-perf/judge-perf-report.mjs scripts/cg-perf/fixtures/perf-report-pass.json` → exit 0、over-budget/fail/condition-unmet/partial → exit 1。

---

## DA批判レビュー記録

### Phase N DA批判レビュー

**DA観点:** （このPhaseで最も壊れやすいポイントは何か？）

| # | 発見した問題/改善点 | 重要度 | 再現手順（高/中は必須） | DA観点 | 対応 |
|---|-------------------|--------|----------------------|--------|------|
| 1 | (具体的に記述) | 高/中/低 | (高/中: 操作→結果) | (どのDA観点で発見したか) | ✅修正済/⏭️別DD/❌不要 |
