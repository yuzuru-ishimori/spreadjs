# DD-021-3: 選択再ベース・Undo整合・P2-1性能

| 作成日 | 更新日 | ステータス | 補足 |
|--------|--------|-----------|------|
| 2026-07-17 | 2026-07-17 | 確認待ち | 親=DD-021（3分割の第3子・実装順3番目）。K3 回収・Undo 生存整合・P2-1 Θ(N²) 是正。実装完了・全検証 green・**Codex レビュー未実施（不可・次セッション要）**・未コミット |

```text
Risk Class: A
Risk Triggers: activeCell/選択の位置表現に触れる（IME 状態機械=editor-state-machine の周辺。再ベース誤りはカーソル下の行実体ずれ=誤入力・入力消失相当）／性能特性の変更（apply.ts のループ構造・replay 経路と共有=退行すると復旧/収束全経路に波及）／undo-stack の実行条件変更（誤緩和はサイレント上書き=R-07）
Human Spec Gate: 解決済（親 DD-021 要確認①④⑤確定=2026-07-17 フル委譲モード）
Codex: 不可（本起票環境・`bash scripts/codex-review.sh --check` exit 1）。実装セッション開始時に再チェックし、利用可なら**必須・effort high**（性能・状態遷移周辺）。**IME 状態機械（editor-state-machine）への遷移追加・位置表現の内部変更が必要と判明したら停止して xhigh へ昇格**（親ヘッダ条項）
Manual Gate: なし（本子DDは synthetic まで。実機 UX は親 Phase 4 M2 に集約）
External Review: なし
Evidence Level: full（A区分=L5。再ベース前後の期待表・性能実測の再現コマンド・線形性の証拠・既知の未保証境界を省略しない）
```

> アプローチ: TDD（再ベース・Undo 整合・性能=正解が明確）＋E2E（リモート挿入下の操作継続）
> 親=**DD-021**。依存: **DD-021-1／DD-021-2 完了**（公開 API・収束シナリオ資産を利用）。完了後は親 Phase 4（統合検証・実測・Manual Gate）へ。

## 目的

K3（行挿入/削除後のローカル選択・Enter 移動先の再ベース）を回収し、行構造変更下でも activeCell・選択レンジ・移動先が**行実体（RowId）に追従**するようにする。あわせて Undo スタックとの整合（削除行に触れるエントリの実行時拒否・除去＋通知=親①）と、P2-1（単一行 InsertRows 連発の Θ(N²)・`apply.ts`）の性能是正を行う。

## 背景・課題（親 DD-021 §背景の該当分）

- **K3 の実体**: `packages/ime/src/editor-state-machine.ts` の `activeCell` と `packages/grid/src/selection-controller.ts` の選択レンジ（anchor/focus）はいずれも**表示 index ベース**（`{row, col}`）。リモート/ローカルの行挿入・削除で `document-view.ts` が rowAxis を再構築しても index は補正されず、カーソル下の行実体がずれる（挿入が上で起きると 1 行下の別行を編集してしまう）。Enter 確定の移動先も同様。
- **Undo 整合**: `packages/grid/src/undo-stack.ts`（DD-020-3）の補償 SetCells は削除行に対し `target-row-deleted` で reject されるが、エントリ処理（除去・通知）と挿入行下での beforeRevision 妥当性が行操作を前提に検証されていない。行操作自体の Undo は対象外（親①=計画書 §15.3 MVP後）。
- **P2-1 の実体**: `packages/core/src/apply.ts` の `nextSlot`（rowMeta **全走査** O(N)）が Insert 1 回ごとに走り、`rowOrder.splice`（O(N)）と合わせ単一行 Insert 連発ログで Θ(N²)（DD-014 既知制約。bulk insert の O(N²) 回避=`replayAcceptedOperations` は実証済み・単発連発が未回収）。replay・rollback 経路と同一関数を共有するため、是正は決定論（同一入力→同一文書）を壊さないことが必須。

## 検討内容

- **再ベース方式（📐 で確定）**: 案(a) editor-state-machine の位置表現を RowId 化＝状態機械の内部変更（大改修・xhigh 昇格トリガー）。案(b)〔既定〕**grid 層の再ベース hook**＝row-structure 変更（`operationDirtyKind === 'row-structure'`）を検知した時点で「変更前に指していた RowId」を `document-view` の新 rowAxis で index へ引き直し、既存 API（状態機械の位置設定・selection-controller の setRange）で補正する（**遷移追加なし・最小侵襲**）。追従用の「現在指している RowId」は grid 層（mount-controller）が所有する。
- **削除時の縮退（親④確定）**: activeCell の行が削除されたら最近傍生存行（下優先→無ければ上）へ移動。選択レンジは生存行へ縮退（全行消失なら単一セルへ）。DD-021-1 のローカル削除縮退を**リモート起因へ一般化**し実装を一本化する。
- **Enter 移動先**: activeCell 再ベースにより自然に解決（移動は再ベース後の index 基準）。編集中（Editing/Composing）のリモート構造変更は ime-editing-session の既存消失検知＋K4（DD-021-2）と整合させ、**編集セルの描画位置**は editor-placement の追従を確認。
- **Undo 生存整合（親①確定）**: 補償 SetCells の**実行前検査**（DD-020-3 の Codex P1b で導入済みの `validateOperation` 事前検査）が `target-row-deleted`/`unknown-row` を検出したらスタックから除去＋`undo-blocked`/`redo-blocked` 通知（既存語彙を流用・新語彙は原則なし）。挿入行は既存エントリへ無影響（beforeRevision 検査は revision ベースで行位置に依存しない）ことをテストで固定。
- **P2-1 是正（📐 で確定）**: `nextSlot` の全走査を **maxSlot の維持**（`SheetDocument` へキャッシュ追加 or rowMeta 更新箇所での単調更新）で O(1) 化。snapshot 復元・clone・un-tombstone 経路での maxSlot 整合が要点（decreasing しない=単調性維持で決定論を保つ）。`rowOrder.splice` O(N) は残る（1,000 連発でも要素移動のみ=実測で目標内なら許容・目標未達なら段階2としてギャップバッファ等を検討）。hash・snapshot round-trip・replay の決定論非退行を必須ガードとする。
- **計測計画（親⑤確定）**: ベンチ=50,000 行文書へ単一行 Insert×1,000 連発（committed 適用経路）で合計時間と per-op 中央値を計測し、行数を変えて線形性を確認。bulk 10,000 行 ≦500ms・replay 非退行（DD-014 計測系を流用）。回帰ガードは緩め（ヘッドルーム 4 倍以上）で常設・厳密値は親 Phase 4 実測記録（DD-020 paste-perf の運用に倣う）。

## 決定事項（親 要確認の確定〔2026-07-17〕の継承）

- **親①**: 行操作自体の Undo/Redo は対象外（計画書 §15.3 維持）。本子DDは Undo スタックの生存整合のみ（削除行エントリ=実行時拒否・除去＋通知）。対象化は Stage 3 バックログ候補。
- **親④**: 選択は生存行へ縮退・activeCell 削除時は最近傍生存行（下優先→上）へ移動。
- **親⑤**: 50,000 行×単一行 Insert×1,000 連発 合計 ≦2s かつ線形（1回 p95 ≦5ms）・bulk 10,000 行 ≦500ms 非退行・replay 非退行。

## 受け入れ基準

| # | 基準（操作 → 期待結果） | 検証方法 |
|---|------------------------|---------|
| 1 | activeCell より上でリモート/ローカル行挿入 → activeCell・選択レンジが同じ行実体（RowId）を指し続ける | Phase 2 unit＋E2E |
| 2 | 挿入/削除後の Enter 確定 → 移動先が表示上の直下の行（行実体とずれない・K3 解消） | Phase 2 unit＋E2E |
| 3 | activeCell の行がリモート削除 → 最近傍生存行（下優先→上）へ移動・選択は生存行へ縮退・クラッシュなし | Phase 2 unit＋E2E |
| 4 | 編集中（IME 含む）にリモート挿入で行位置が変わる → 編集セルの描画位置が追従・ドラフト非破壊 | Phase 2 synthetic＋IME 不変条件 |
| 5 | 削除行に触れる Undo/Redo → 実行前検査で拒否・スタック除去・`undo-blocked`/`redo-blocked` 通知（サイレント上書きなし） | Phase 3 unit＋E2E |
| 6 | 行挿入は既存 Undo エントリへ無影響（挿入後も Undo が正しい前値へ戻す） | Phase 3 unit |
| 7 | 50,000 行文書で単一行 Insert×1,000 連発 → 合計 ≦2s・行数に対し線形（Θ(N²) 解消） | Phase 1 ベンチ（再現コマンド記録）＋親 Phase 4 実測 |
| 8 | bulk insert 10,000 行 ≦500ms・replay/snapshot round-trip/hash の決定論非退行 | Phase 1 ベンチ＋既存テスト green |

## タスク一覧

### Phase 0: 事前精査
- [x] 📋 **各Phaseのタスク精査・詳細化**（AC⇔検証対応・対象パス・変更内容の具体性・🔬の有無）
- [x] 📐 **実装前詳細化**（判定=要）: 下記「📐 実装前詳細化（確定）」に記録。案b（grid 層 hook・状態機械無変更）で成立＝xhigh 昇格せず。maxSlot 維持点（clone/snapshot/un-tombstone）・ベンチ経路（replayAcceptedOperations）確定
- [x] 🧑‍⚖️ **Codexレビュー要否判定**: 実装セッション冒頭で `bash scripts/codex-review.sh --check` 再実行 → **exit 1（不可・codex CLI なし）**。**Codex レビューは次セッションで必須・effort high**（状態機械変更は発生していないため xhigh 不要）
- [x] 😈 **Devil's Advocate調査**（再ベースと rollback/replay の交錯順序・maxSlot キャッシュの snapshot 互換・「線形」の測定ノイズ）→ 下記 DA 記録

### Phase 1: P2-1 性能是正（TDD・core）
- [x] `packages/core/src/apply.ts`・`document.ts`: `nextSlot` の全走査を maxSlot 維持で O(1) 化（clone・snapshot 復元＝`computeMaxSlot`・単調非減少）。`SheetDocument.maxSlot` 追加・`nextSlot` 廃止
- [x] ベンチテスト新設: `apps/pocd-bench/src/bench-row-insert.ts`（`npm run bench:row-insert`）＋ CI 常設の緩い回帰ガード `packages/core/src/apply-perf.test.ts`（決定論＋2s 天井）
- [x] 🔬 **機械検証**: `npm run test`（core 156・全 998 green・hash/snapshot/replay/differential 含む）＋ベンチ実行 → 親⑤目標内・N 非依存性（indexOf 残存で per-op が N に緩く比例するが目標内）を確認
- [x] 😈 **DA批判レビュー**（下記記録）

### Phase 2: K3 選択・activeCell 再ベース（TDD＋E2E）
- [x] `packages/grid/src/mount-controller.ts`: masterLoop の構造 flush を bracket する `captureRebaseState`/`applyRebaseState` hook を追加。RowId を旧 Axis から採取→新 Axis の index へ引き直し既存 API（`pointerdownCell`/`selectionCtrl.rebaseRows`）で補正。**DD-021-1 の `reduceActiveCellAfterDelete`（ローカル削除専用）を撤去し、ローカル/リモート共通経路へ一本化**
- [x] `packages/grid/src/selection-controller.ts`: `rebaseRows(resolveRow)` 追加（両端 RowId 追従・生存皆無で単一化）。`packages/grid/src/row-operations.ts`: 純関数 `rebaseRowIndex`（Insert/Delete 統合・下優先→上フォールバック）追加
- [x] `ime-editing-session.ts`: 変更不要を確認（`refreshPlacement` は既に editingTarget=RowId ベースで追従。状態機械・IME 無変更・AC4 は既存機構で成立）
- [x] `apps/playground/e2e/row-rebase.spec.ts`（新設）: AC1〜4（standalone・決定的）＋リモート AC1（collab・同一 hook 確認）
- [x] 🔬 **機械検証**: `npm run test`(998)・`npm run test:invariants`(57)・関連 E2E（row-rebase 7・row-operations/undo/range 24・bootstrap/standalone 9）全 green（IME 不変条件含む）
- [x] 😈 **DA批判レビュー**（下記記録）

### Phase 3: Undo 生存整合（TDD＋E2E）
- [x] 実行前検査は DD-020-3 の `submitCompensation`（`validateOperation(committed, op)`）が既に `target-row-deleted`/`unknown-row` を検出し `blockInFlightCompensation`（エントリ除去）＋`undo-blocked`/`redo-blocked` 通知する経路を**確認**。行構造変化で undo スタックは壊れず、undo 実行時に self-heal（新規コード不要＝既存機構が AC5 を満たす）
- [x] unit＋E2E: AC5（削除行 Undo 拒否・スタック除去・行非復活）・AC6（挿入無影響・RowId ベース前値復元）を row-rebase.spec.ts で追加。validate/undo-stack の既存 unit も green
- [x] 🔬 **機械検証**: `npm run test && npx playwright test`（該当スペック）→ 全 green
- [x] 😈 **DA批判レビュー**（下記記録）

## 📐 実装前詳細化（確定・2026-07-17）

- **再ベース方式＝案b（grid 層 hook・状態機械無変更）で成立**（xhigh 昇格せず）。所有: 「今どの RowId を指すか」は grid 層が
  masterLoop の構造 flush を跨いで採取する（`SheetDocument`/editor-state-machine を持ち回らない）。
  - **capture（flush 前）**: 旧 rowAxis の RowId 列 `oldOrder`、activeCell（**Navigation 位相かつ非 composition のときだけ**＝
    編集中は editingTarget ベースの `refreshPlacement` が追従・`pointerdownCell` は commit を誘発するため触らない・I-3）。
  - **apply（flush 後）**: 新 rowAxis の `newOrder` へ `rebaseRowIndex(oldOrder,newOrder,row)` で引き直し。生存→新 index、削除→
    最近傍生存行（下優先→上・親④）、生存皆無→null（選択解除）。選択レンジは `selectionCtrl.rebaseRows` で両端追従。
  - **初回 bootstrap 構築（`firstDataDrawn=false`）・空 Axis は再ベースしない**（「指していた行」が無い新規構築）。
- **一本化**: DD-021-1 の `reduceActiveCellAfterDelete`（ローカル削除専用・同期）を撤去。ローカル/リモート問わず構造 flush の
  同一 hook が縮退する（`performDeleteRows` は個別処理を持たない）。ローカルは次 rAF で縮退（E2E は expect.poll でゲート・成立）。
- **maxSlot 維持点**: `createDocument`(-1)／`cloneDocument`(コピー)／`deserializeDocument`(=`computeMaxSlot`＝rowMeta 最大)／
  `applyInsertRows`(採番連番の末尾で一括更新)。**単調非減少**（rollback で行が消えても減らさない＝slot 一意性維持で決定論を保つ）。
  hash 非依存（正準形は slot 非依存）ゆえ round-trip/replay の hash 不変。wire（DocumentSnapshot）は無変更＝maxSlot は導出。
- **ベンチ経路**: P2-1 が是正するのは **`replayAcceptedOperations`（restart 復旧・snapshot tail replay）**。1 op ごとに全文書 clone する
  immutable な `applyOperation` の O(N)/op は DD-014 既知性質（P2-1 スコープ外＝snapshot が要る根拠）で触らない。

## P2-1 実測（再現コマンド付き・2026-07-17）

- **再現**: `cd apps/pocd-bench && npm run bench:row-insert`（`-- --pretty` で整形）。経路=`replayAcceptedOperations`。
- **親⑤目標（50,000 行 + 単一行 Insert×1,000 連発）**: 合計 **124ms**（目標 ≦2s）・per-op p95 **0.176ms**（目標 ≦5ms）→ **pass（約 15 倍ヘッドルーム）**。
- **bulk 10,000 行 Insert**: **2.8ms**（目標 ≦500ms）・**決定論一致（hash 同一）**→ pass。
- **線形性（初期行数 N を 4 段）**: 1k=3.8ms / 5k=22ms / 25k=69ms / 50k=171ms（per-op 0.004→0.171ms）。
  → 合計は N に**線形**（旧 nextSlot 全走査の二乗は解消）。ただし per-op が N に緩く比例するのは **`resolveAnchorIndex` の
  `rowOrder.indexOf`（アンカー探索 O(N)）＋`splice`** が残るため（後述 DA/残課題）。
- 決定論の非退行は `packages/core/src/apply-perf.test.ts`（CI 常設・2s 天井）＋既存 differential/hash/snapshot/invariants で担保。

## ⚠️ P2-1 の残存事項（DD の根本原因記述の補正・要判断ではないが記録）

- DD 本文は Θ(N²) の原因を「`nextSlot` 全走査＋`splice`」と記述していたが、実測により **`resolveAnchorIndex` の
  `rowOrder.indexOf(afterRowId)`（O(N)）も同等の残存要因**と判明。本子DDは 📐 どおり `nextSlot` を maxSlot キャッシュで O(1) 化した
  （これが指定された修正）。`indexOf`＋`splice` は配列ベース `rowOrder` に内在し、O(1) 化には順序構造/gap buffer 等の**大改修**が要る
  （DD 📐 が「splice O(N) は残る・目標内なら許容／未達なら段階2」と明記）。**親⑤目標は満たすため段階2は起票せず**、
  `doc/plan/stage2-backlog.md` の P2-1 行に「indexOf も残存＝順序構造化は段階2候補」を追記候補として引き継ぐ。

## 引き継ぎ物（→ 親 DD-021 Phase 4）

- K3 解消（RowId 追従の activeCell/選択・Enter 移動先）＝親 AC5 の充足材料。
- 線形化された apply（P2-1）＋ベンチハーネス・実測データ＝親 Phase 4 の性能実測（AC8）が再利用。
- Undo 生存整合＝親 AC7 の充足材料。既知制約（行操作 Undo 対象外=Stage 3 候補）の明文化。

## ログ

### 2026-07-17
- DD作成（親 DD-021 の Phase 3 を自己完結の子DDとして起票。親 要確認①④⑤の確定値を決定事項へ転記=フル委譲モード）。
- 再ベース方式は案b（grid 層 hook・状態機械無変更）を既定として 📐 で確定する（DD-020-1 の「案X=selection-controller を状態機械の外へ」先例と同方向の最小侵襲）。
- Codex 利用可否: 不可（本起票環境）。実装セッションで再チェック（ヘッダ参照）。

### 2026-07-17（実装セッション・Opus）
- **Codex 再チェック**: `bash scripts/codex-review.sh --check` → **exit 1（利用不可・codex CLI 無し）**。スキップ。**Codex レビューは次セッションで必須・effort high**（Risk Class A・性能/選択/Undo 周辺。状態機械の遷移追加は発生していないため xhigh 不要）。勝手に完了扱いにしない。
- **Phase 1（P2-1・core）**: `SheetDocument.maxSlot` 追加・`nextSlot` 全走査を廃止し `applyInsertRows` を O(1) 採番へ。`createDocument`/`cloneDocument`/`deserializeDocument`（`computeMaxSlot`）で維持。単調非減少で決定論維持。ベンチ `bench-row-insert` 新設・回帰ガード `apply-perf.test.ts` 新設。**apply/replay/differential/hash/snapshot 既存テスト全 green（非退行）**。実測は上記「P2-1 実測」。
- **Phase 2（K3 再ベース・grid）**: masterLoop 構造 flush を bracket する capture/apply hook 追加。`rebaseRowIndex`（純関数）・`selectionCtrl.rebaseRows` 追加。`reduceActiveCellAfterDelete` を撤去し一本化。IME 状態機械・ime-editing-session は無変更（AC4 は既存 editingTarget placement で成立）。**protocol 無変更・状態遷移追加なし**。
- **Phase 3（Undo 生存整合）**: DD-020-3 の `submitCompensation` 事前 `validateOperation` が既に削除行を `target-row-deleted` で拒否・エントリ除去・block 通知する経路を確認（新規コード不要）。AC5/6 を E2E 追加で固定。
- **検証結果**（すべて exit 0 確認）: `npm run test`=**998 pass**／`npm run test:invariants`=**57 pass**（row-heavy collab で maxSlot 決定論を fault 注入下も確認）／`npm run typecheck`=green／`npm run lint`=green（boundary new=0・bench ファイルを R1 baseline 追加）／facade `.d.ts` snapshot=変化なし（公開 API 型差分なし）。E2E: `row-rebase.spec.ts`（AC1〜6＋リモート AC1）=7 pass、既存 `row-operations`/`undo-redo`/`range-selection`=24 pass（無退行）、`reload-bootstrap`/`standalone`/`clipboard-standalone`=9 pass（deserialize maxSlot 経路確認）。
- **maxSlot 追加に伴う SheetDocument リテラル更新**: production 3 箇所（createDocument/cloneDocument/deserialize）＋テスト/ベンチ 4 箇所（`document.test.ts`・`apply.test.ts`・`hash.test.ts`×2・`bench-replay.ts`）。tsc が漏れを検出（テスト実行=vitest は型を検査しないため typecheck exit code で確認必須）。
- **要判断**: なし（案b で成立・状態機械/protocol 無変更）。P2-1 の根本原因に `resolveAnchorIndex.indexOf`（O(N)）が残る点を発見したが、親⑤目標は満たすため段階2は起票せず（上記「残存事項」参照）。
- **未実施**: Codex レビュー（不可）・実機 IME/UX（親 Phase 4 Manual Gate）。コミット/アーカイブはオーケストレータのユーザー確認後。

### 2026-07-17（外部レビュー: Fable 5 で Codex high を代替・ユーザー決定）
- Codex CLI が本環境で利用不可のため、**ユーザー決定（2026-07-17）で Fable 5 レビューを Codex high の代替**とした。独立レビューア（Fable 5）が本コミット差分（782743f）をレビューし、**maxSlot の全更新経路 8 系統**（createDocument/cloneDocument/applyInsertRows/applyDeleteRows〔tombstone=rowMeta 不削除で単調維持〕/deserializeDocument〔tombstone 込み computeMaxSlot＝採番カーソルと厳密一致〕/applyInverseSeed〔下げない=slot skip は衝突を生まない・hash は slot 非依存〕/テスト・ベンチ literal/rowOrder 全数 grep）と replay 決定性（fresh replay と snapshot+tail replay の slot 一致・doc-compare の slot 比較整合）を検証・**バグなし**。Undo 生存整合の「新規コード不要」判断も beginUndo→validateOperation→blockInFlightCompensation→notifyCompensationBlocked の到達性を追認。ヘッダの「Codex レビューは次セッションで実施要」はこれをもって充足と読み替える。
- findings と採否:
  - **[P2 反映] ドラッグ中の選択（dragAnchor/dragFocus）が K3 再ベースの対象外**（ドラッグ中にリモート構造 Op が flush されると旧 index のまま pointerup で確定＝ずれた行帯を削除/コピーしうる。リサイズドラッグの Id 保持修正〔Codex P2〕と同型の欠落）→ `selectionCtrl.rebaseRows` でドラッグ状態も再ベース（行消失時はドラッグ取消）。unit 3 ケース追加。
  - **[P3 反映] 楽観 insertRows の reject 時に structure dirty が立たず Axis に幽霊行が残る**（既存バグだが K3 が「Axis=正」を前提に一本化した今、影響面が拡大）→ `session-sync.ts` の operationRejected で `markStructureDirty()` を追加（reject は稀ゆえ一括）。
  - **[P3 反映] `reduceActiveRowTarget` が本番コードから死亡**（rebaseRowIndex への一般化で置換済み・テストのみ参照）→ 関数とテストを削除（rebaseRowIndex 側が全ケースを網羅済み）。
  - **[P3 記録] apply-perf.test の時間ガード 2s は遅い共有 CI で余裕 1.5〜3 倍**（per-op O(N) の indexOf が支配項）: fail 側に倒れるだけで正当性リスクなし。flaky 化したら linearity 比の構造ガードへ置換する（残存事項に追記済みの indexOf 段階2と同時に扱う）。
  - **[情報 記録] ベンチの「本番経路」主張の射程**: replayAcceptedOperations（restart 復旧/snapshot tail replay）に限れば真。対話編集経路（submitLocalOperation→rebuildView・op ごと全文書 clone）は別性質で、128ms を対話性能と読まないこと（bench meta.note に明記済み＝誠実と確認）。
- 反映後の検証: `npm run test` 997 pass／invariants 57／typecheck・lint（boundary new=0）／**playground E2E 全スイート 67 pass**／showcase 3 pass。

---

## DA批判レビュー記録

**DA観点:** 再ベースと rollback/replay の交錯順序・maxSlot キャッシュの snapshot 互換・「線形」の測定ノイズ・Undo と行構造変化の交錯。

| # | 発見した問題/改善点 | 重要度 | 再現手順（高/中は必須） | DA観点 | 対応 |
|---|-------------------|--------|----------------------|--------|------|
| 1 | maxSlot を rollback（applyInverseSeed の行除去）で減らすと、後続 Insert が旧 slot を再利用し scan 版と slot 番号が食い違う恐れ | 中 | insert→rollback→再 insert で slot 比較 | snapshot 互換/決定論 | **単調非減少**に確定（減らさない）。slot は hash 非依存かつ committed は rollback ghost を含まない（view のみ）ため round-trip/replay の hash 不変。differential・collab invariant（fault 注入）で非退行確認 |
| 2 | 初回 bootstrap の構造 flush で activeCell を「生存行なし」誤判定し選択解除する恐れ | 高 | 初回ロード時の rowAxis 構築 | 再ベース暴発 | `firstDataDrawn=false`・空 oldOrder では再ベースしない guard を追加。bootstrap/standalone E2E で無退行確認 |
| 3 | 編集/変換中に構造 flush が `pointerdownCell` を呼ぶと commit 誘発＝draft 破壊・IME 干渉（I-3 違反） | 高 | 変換中にリモート/ローカル挿入 | IME 不変条件 | capture 時に **Navigation 位相かつ非 composition** のみ activeCell を対象化。編集セルは editingTarget=RowId の placement が追従。AC4 E2E＋IME 不変条件テストで確認 |
| 4 | 選択レンジ再ベース後に activeCell 補正の `pointerdownCell`→onChange→`syncWithEditor` がレンジを誤解除する | 中 | 明示レンジありで構造変更 | 順序依存 | anchor===activeCell 不変条件下では両端が同一 RowId→同一新 index に再ベースされ `sameCell` 成立で非解除。レンジ再ベースを activeCell 補正の**前**に実施 |
| 5 | 「線形」判定の測定ノイズ／CI マシン速度差で時間ガードが flaky | 中 | CI で bench 常設 | 測定信頼性 | CI 常設は**決定論**（hash/slot 一意）＋**緩い天井（親⑤上限 2s＝実測の約 15 倍）**のみ。厳密値は手動 `bench:row-insert` を DD 記録に限定 |
| 6 | P2-1 の Θ(N²) 原因が nextSlot だけでなく `resolveAnchorIndex.indexOf`＋splice にもある | 中 | bench で per-op が N に比例 | 目標未達リスク | 実測で親⑤目標は pass（124ms/2s）を確認。indexOf/splice は配列内在＝段階2候補として明記（残存事項）。本子DDは 📐 指定の nextSlot O(1) 化を完了 |
