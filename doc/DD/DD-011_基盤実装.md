# DD-011: 基盤実装

| 作成日 | 更新日 | ステータス | 補足 |
|--------|--------|-----------|------|
| 2026-07-13 | 2026-07-13 | 検討中 | 要確認①〜④回答済（2026-07-13）。前提=子DD DD-011-1（packageリネーム）完了後に着手 |

```text
Risk Class: B
Risk Triggers: 基盤整備（機械的）。公開面の骨格（Facade skeleton・contract test）に触れるが、承認済み境界（DD-009 package-boundary.md）に沿う設置のみで公開APIの実質確定はしない
Human Spec Gate: skipped（承認済みバックログ範囲・roadmap §2.1。ただし本DD内容はオーケストレータがユーザーへ提示）
Codex: high 1回（B区分標準・roadmap §2.2 L3。Phase 5 で全差分一括）
Manual Gate: なし（機械的・自動検証のみ。実機/headed 不要）
External Review: 不要（Phase境界・API確定・ADR転換・Go/No-Go に該当せず）
Evidence Level: standard（5点圧縮証跡・roadmap §2.2 L5）
```

> アプローチ: 標準（lint ルール・テスト runner・harness の基盤整備が中心のため。個々のルールは Phase 内で検証タスクと対で実装）
>
> **B→A 昇格条件（roadmap §2.4・必読）**: 実装中に次のいずれかが判明したら**作業を停止して Risk Class A へ昇格**し、ユーザーへ提示する — 受け入れ基準の変更／データ形式・protocol 変更への波及／永続化境界への波及／Internal 予定 API を consumer へ確定露出（Facade skeleton が stub を超えて実 API を固定し始めた場合を含む）／1DDで複数の状態所有者を変更。

## 目的

Stage 1 SDK Alpha の縦切りDD群（DD-012〜018）が乗る**共通基盤を機械的に設置**する（roadmap §3 DD-011）:
package skeleton／package boundary lint（`apps/*` 間・内部相対 import の恒久禁止）／contract test 骨格／常設不変条件スイート runner（§2.3）／independent consumer harness（§7）／Phase 1 用 DD 差分テンプレ改修（Risk Class ヘッダ＋製品化6観点）。

## 背景・課題

- DD-009 が package 責務境界（`doc/archived/DD/DD-009/package-boundary.md` §4 = **boundary lint の実装仕様の正本**）と資産台帳を確定済み。DD-010 が CG-2（安定ID）を解除済み。次の DD-012（IME縦切り）から実抽出が始まるため、**抽出前に境界の機械ガードと常設スイートの受け皿が必要**。
- **依存: 子DD DD-011-1（packageリネーム）→ 本DD**（要確認④のユーザー決定 2026-07-13＝「今 rename する・子DDへ切出し」）。本DDの skeleton・boundary lint・contract test は **rename 後の最終名 `@nanairo-sheet/{types,core,collab,server,formula}`（ディレクトリも `packages/{types,core,collab,server,formula}` へ統一済み）の上に構築**する。前提訂正: scope 付与は D-003 で実施済みのため rename の実体は `sheet-` プレフィックス除去＋ディレクトリ名統一（詳細は `DD-011-1_packageリネーム.md`）。
- 現行 `eslint.config.js` は基本設定のみ（DD-001 時点で「boundary lint は PoC-C 以降に導入」と明記＝本DDが回収）。
- 現行 `apps/playground`・`apps/collaboration-server` は内部パッケージを直接 import している（抽出未完のため正常）。R1 を即 full-error にすると green を維持できない → DD-009 §4.3 の**段階的導入（baseline 方式）**に従う。
- S1-3（独立 consumer 実証）は fixture 不合格（roadmap §7）。pack 済み成果物経由でインストールできる独立 consumer プロジェクトの雛形を DD-016 までに用意する必要がある。

## 検討内容

1. **boundary lint の実装方式**: ESLint flat config の `no-restricted-imports`/カスタム判定（R1/R2/R3/R5）＋専用スクリプト（R4 の package root 脱出判定・R7 の AST/型情報検査。境界文書 §4.2 注記どおり **R7 は正規表現のみの実装は不可**）。R6 は既存 `typecheck:core`（ADR-0022）を流用し新規実装しない。
2. **不変条件スイートの置き場**: (a) 専用 workspace package を新設 ／ (b) ルート `tests/` ディレクトリ＋既存 vitest から実行。→ **(b) を採用**。workspace 追加を最小化し package-lock 変更を抑える（並行セッション lock 安全）。既存 `packages/*/src/test-support.ts`（collab/server）と `sim/remote-update-simulator.ts`（台帳 Q6: DD-011/013 で再判断）を素材に流用。
3. **consumer harness の形態**: 独立プロジェクト（**workspaces 対象外**のディレクトリ。`npm pack` tarball からインストール）。monorepo workspace link・source path 直接参照は S1-3 不合格条件のため使わない。実在社内アプリへの統合は DD-016 の担当。→**要確認③＝雛形で確定**（2026-07-13 ユーザー回答）。
4. **DD差分テンプレ改修**: `doc/templates/` は `dd-update.sh`（dd-know-how からの Pull 更新）の上書き対象のため、直接改修は DD-008 要確認(b) で一度**非採用**とした先例がある。製品化6観点は既に `doc/plan/phase0-dd-roadmap.md` に存在。反映先の選択肢: (a) `doc/templates/` を直接改修（次回 dd-update で差分衝突リスク）／(b) プロジェクト管理のヘッダ雛形を `doc/plan/` 等 dd-update 非管理の場所へ新設し各DDがコピー／(c) 上流 dd-know-how へ反映（本リポジトリ外作業）。→**要確認①＝(b) で確定**（2026-07-13 ユーザー回答。templates 直接改修はしない・DD-008 先例と整合）。
5. **package rename（`sheet-*` プレフィックス除去 → DD-009 論理名）**: DD-009 は「判断と実行は DD-011」とした（強制はしていない）。今 rename すると全 import・package-lock の大変更（並行セッションと衝突しうる）。見送ると lint は論理名↔現行名マップで運用（境界文書 §4.3 移行期方式）＝後日 rename 時に lint 設定・baseline・contract test の二重手戻り。→**要確認④＝「今 rename する」で確定**（2026-07-13 ユーザー回答）。**子DD `DD-011-1_packageリネーム.md` へ切り出し済**（`@nanairo-sheet/sheet-*` → `@nanairo-sheet/{types,core,collab,server,formula}`＋ディレクトリ名も統一）。これにより**論理名↔現行名マップは不要**になり、本DDは最終名の上に直接構築する。新設 Facade は最初から `@nanairo-sheet/grid`・`@nanairo-sheet/server-hono`（D-003 整合）。

## 決定事項

- **前提依存: DD-011-1（packageリネーム）→ 本DD**。boundary lint の R1 判定名は DD-009 論理名＝**rename 後の最終名 `@nanairo-sheet/{types,core,collab,server,formula}` と一致**させる（DD-011-1 完了により境界文書 §4.3 の論理名↔現行名マップ〔移行期方式〕は**不要**）。
- boundary lint のルール定義は **DD-009 `package-boundary.md` §4（R1〜R7）を正本**とし、本DDは実装のみ（規約の新規発明をしない）。
- 段階的導入は境界文書 §4.3 のとおり: **R3/R4/R5 は最初から全体 full-error**・**R1 は consumer harness に full-error 先行適用＋既存 `apps/*` は期限付き baseline（既知例外リスト。新規違反のみ ERROR）**・R6 は既存 `typecheck:core` 流用・R7 は AST/型情報ベース。baseline の縮退は各抽出DD（DD-012〜016）、ゼロ確認は DD-018（S1-1）。
- 不変条件スイートはルート `tests/invariants/`（IME・共同編集・公開API・性能予算の4カテゴリ）＋ `npm run test:invariants`。**本DDは runner と最小ケースまで**。各カテゴリの実充足は担当縦切りDD（roadmap §2.3）。
- contract test 骨格は Facade 公開面（export surface＋公開シグネチャ）の snapshot 検査。Facade skeleton の stub 面を初期 snapshot とし、意図しない export 追加/削除/型変更で fail する枠組みまで（実 API の確定は DD-016）。
- codec 移設（rename 後パス: `packages/collab/src/message-codec.ts` → `packages/core/src/`）は DD-009 Codex P1 反映済みの承認判断（R3 逆流回避）のため本DDで実施（挙動保存・テスト green 維持）。
- Facade skeleton（`@nanairo-sheet/grid`・`@nanairo-sheet/server-hono`＝最終名を直採用・D-003 整合）は **mount/serve の stub＋型の置き場まで**。実装は DD-012〜016。stub が実 API を固定し始めたら B→A 昇格条件に該当。
- workspace 追加・package-lock 変更を伴うタスクは、着手前に `git status`＋並行セッション確認を行う（メモリ「並行セッションとlock安全」）。

**要確認の確定（2026-07-13 ユーザー回答・全4件解決済）**:

| # | 要確認 | 確定内容 |
|---|---|---|
| ① | DD差分テンプレの**実ファイル改修可否**（DD-007 要確認3＝dd-update 管理系との整合） | **別ファイル新設で確定**: dd-update.sh の上書き対象外の `doc/plan/dd-risk-class-header.md`（新設）へ Risk Class ヘッダ雛形＋製品化6観点を置く。`doc/templates/` の直接改修はしない（DD-008 要確認(b) の先例と整合） |
| ② | boundary lint の**厳格度と既存コードへの影響**（R4/R5 の既存違反の扱い） | **機械的修正で済む既存違反（R4 境界越え相対 import・R5 apps間）は本DDで是正**。大きい構造変更が要るものは baseline 化して担当DD（DD-012〜016）へ送る |
| ③ | consumer harness を**実在アプリにするか雛形に留めるか** | **雛形で確定**（pack 経由の独立プロジェクト）。実在社内アプリ統合（S1-3 本実証）は DD-016 |
| ④ | **package rename の要否・時期** | **「今 rename する」で確定・子DD `DD-011-1_packageリネーム.md` へ切り出し済（dir も rename）**。前提訂正: scope 付与は D-003 で実施済みのため、実体は `@nanairo-sheet/sheet-*` → `@nanairo-sheet/{types,core,collab,server,formula}` のプレフィックス除去＋ディレクトリ名統一（`packages/sheet-core`→`packages/core` 等）。依存は **DD-011-1 → DD-011**（本DDは rename 後の最終名の上に構築・論理名↔現行名マップ不要） |

## 受け入れ基準

| # | 基準（操作 → 期待結果） | 検証方法 |
|---|------------------------|---------|
| 1 | `npm run lint` → R3/R4/R5 全体 full-error・R1 は harness full-error＋既存 `apps/*` baseline 方式で green（新規違反0・baseline は既知例外リストに明示） | Phase 2 🔬 |
| 2 | 違反フィクスチャ（apps間 import／package 境界越え相対 import／consumer からの内部直接 import／Facade の内部 re-export）を一時作成 → lint/検査が ERROR を報告 | Phase 2 🔬（否定テスト） |
| 3 | `npm run test:invariants` → IME・共同編集・公開API・性能予算の4カテゴリ runner が最小ケースで green | Phase 3 🔬 |
| 4 | Facade 公開面 snapshot（contract test）がある状態で export を追加/削除 → contract test が fail し、snapshot 更新手順で復旧できる | Phase 3 🔬（否定テスト） |
| 5 | `npm pack` した tarball を独立 consumer プロジェクトへインストール → import＋`tsc --noEmit` green（workspace link・source path 参照・内部 package 直接 import なし） | Phase 4 🔬 |
| 6 | 既存回帰なし: `npm run test`・`npm run typecheck`・`npm run lint` 全 green（DD-010 時点 561 tests 維持） | 各Phase 🔬 |
| 7 | Risk Class ヘッダ＋製品化6観点チェックが `doc/plan/dd-risk-class-header.md`（要確認①確定の反映先）に存在し、DD-012 以降が参照できる | Phase 5（反映先の実在確認） |

## タスク一覧

### Phase 0: 事前精査
- [ ] 📋 **各Phaseのタスク精査・詳細化**（AC の検証対応・対象ファイルパス・🔬タスクの有無を確認）
- [ ] 📐 **実装前詳細化トリガー判定**: 規模シグナル該当（3ファイル以上・新規モジュール追加）。判定結果: `Phase 1〜4 → 詳細化要（各Phase冒頭で軽量に実施・boundary 仕様は DD-009 §4 正本のため再設計不要）／Phase 5 → 不要`
- [ ] 🧑‍⚖️ **Codexレビュー要否判定**: 判定結果: `全Phase一括で Phase 5 に1回・推奨・effort: high（B区分標準〔roadmap §2.2 L3〕・3ファイル以上変更の推奨シグナル該当。xhigh 条件に非該当）`。Codex 利用可確認済み（2026-07-13 `--check` exit 0）
- [ ] 😈 **Devil's Advocate調査**（baseline 方式が「恒久免罪符」化しないか／stub Facade が早すぎるAPI固定にならないか／harness 雛形が S1-3 の実証を先送りしすぎないか）
- [x] 要確認①〜④のユーザー回答を反映（2026-07-13 全4件確定・決定事項の確定表参照。④により子DD DD-011-1 起票済＝本DDの前提）
- [ ] DD-011-1（packageリネーム）の**完了確認**（rename 済・test/typecheck/lint 全 green・旧名参照 0）を本DD着手の前提条件として確認

### Phase 1: package skeleton（Facade 骨格・codec 移設）

> **前提: DD-011-1（packageリネーム）完了後に着手**。以下のパスはすべて rename 後の最終形（`packages/{types,core,collab,server,formula}`）。

- [ ] 着手前確認: DD-011-1 完了＋`git status`＋並行セッション有無（package-lock 変更を伴うため。メモリ「並行セッションとlock安全」）
- [ ] `packages/grid/`（package 名= `@nanairo-sheet/grid`・最終名直採用）新規: mount/destroy stub＋公開型の置き場（`src/index.ts`・`package.json`・`tsconfig.json`。ランタイム依存ゼロ維持）
- [ ] `packages/server-hono/`（package 名= `@nanairo-sheet/server-hono`・最終名直採用）新規: serve/stop stub＋公開型の置き場
- [ ] `packages/collab/src/message-codec.ts` を `packages/core/src/` へ移設し、`packages/collab`・`apps/collaboration-server/src/server.ts` の import を追随（挙動保存・DD-009 Codex P1 反映済み判断）
- [ ] ルート `package.json`: 新 workspace の追加が不要か確認（`packages/*` glob で自動包含）・`npm install` で lock 差分を最小確認
- [ ] 🔬 **機械検証**: `npm run test && npm run typecheck && npm run lint` → 全 green（561 tests 維持・codec 移設の回帰なし）
- [ ] 😈 **DA批判レビュー**（stub の公開面が DD-016 の設計余地を奪っていないか・codec 移設の取りこぼし import はないか）

### Phase 2: package boundary lint（R1〜R7・baseline・否定テスト）
- [ ] `eslint.config.js`: R1（consumer→内部禁止）・R2（Facade間禁止）・R3（依存逆流禁止）・R5（apps間禁止）を DD-009 §4.1 許可表から実装（判定名は DD-009 論理名＝**rename 後の最終名をそのまま使用**。DD-011-1 完了により論理名↔現行名マップ〔§4.3 移行期方式〕は不要）
- [ ] `scripts/boundary/`（新規）: R4（package root 脱出の相対 import 検出）・R7（Facade の内部 re-export／公開シグネチャへの内部型漏洩を AST/型情報で検査。正規表現のみは不可）を実装し `npm run lint:boundary` として配線
- [ ] R1 の baseline: 既存 `apps/playground`・`apps/collaboration-server` の現行違反を既知例外リスト（`scripts/boundary/baseline.json`）へ固定化（新規違反のみ ERROR・縮退責務は DD-012〜016・ゼロ確認は DD-018）
- [ ] R4/R5 の既存違反スキャン → 検出結果を本DDログへ記録し、要確認②の確定どおり処理（**機械的修正で済むものは本DDで是正**・大きい構造変更が要るものは baseline 化して担当DD〔DD-012〜016〕へ送る）
- [ ] 🔬 **機械検証**: `npm run lint && npm run lint:boundary` → green（AC1）。違反フィクスチャ4種を一時作成し ERROR 検出を確認後削除（AC2）
- [ ] 😈 **DA批判レビュー**（baseline に入れてはいけない新規違反が混ざっていないか・旧 package 名の参照が lint をすり抜けて残っていないか〔DD-011-1 の grep 0 と突合〕）

### Phase 3: 常設不変条件スイート runner＋contract test 骨格
- [ ] `tests/invariants/{ime,collab,api,perf}/`（新規・ルート直下）: 4カテゴリの runner 構成＋各カテゴリ最小ケース1本以上（素材: `packages/*/src/test-support.ts`・`apps/playground/src/sim/remote-update-simulator.ts`〔台帳 Q6〕。roadmap §2.3 の不変条件一覧をカテゴリ README に転記し、充足責務DDを明記）
- [ ] ルート `package.json`: `test:invariants` script 追加（vitest 対象に `tests/` を含める設定）
- [ ] `tests/contract/facade-surface.test.ts`（新規）: Facade（grid・server-hono）の export surface snapshot 検査＋snapshot 更新手順のドキュメント化（意図的変更は snapshot 更新＋CHANGELOG が必要である旨）
- [ ] 🔬 **機械検証**: `npm run test:invariants` → 4カテゴリ green（AC3）。export を一時追加し contract test fail→snapshot 更新で復旧を確認（AC4）
- [ ] 😈 **DA批判レビュー**（最小ケースが「動くだけで何も守らない」形骸になっていないか・perf 予算スモークの閾値根拠）

### Phase 4: independent consumer harness（雛形）
- [ ] `consumer-harness/`（新規・**workspaces 対象外**・独自 package.json/lock）: `npm pack` した tarball（grid・server-hono）をインストールし、mount stub の import＋型利用だけを行う最小 TS プロジェクト（要確認③確定=雛形。実在アプリ統合〔S1-3 本実証〕は DD-016）
- [ ] `scripts/consumer-harness.sh`（新規）: pack→install→`tsc --noEmit` を一括実行する検証スクリプト（S1-3 不合格条件〔workspace link・source path 参照・内部直接 import〕を含まないことをスクリプト内で確認）
- [ ] harness に R1 full-error の boundary lint を先行適用（DD-009 §4.3-1）
- [ ] 🔬 **機械検証**: `bash scripts/consumer-harness.sh` → pack→install→typecheck green（AC5）
- [ ] 😈 **DA批判レビュー**（雛形が fixture の言い換えになっていないか＝DD-016 の実証と何が違うかを README に明記）

### Phase 5: DD差分テンプレ改修＋Codexレビュー＋証跡
- [ ] `doc/plan/dd-risk-class-header.md`（新規・要確認①確定の反映先）: Risk Class ヘッダ雛形（roadmap §2.1）＋製品化6観点チェック（`doc/plan/phase0-dd-roadmap.md` の6項目）を設置し `doc/DOC-MAP.md` へ追記（`doc/templates/` は改修しない＝dd-update 上書き対象外を維持・DD-008 先例整合）
- [ ] `DD-011/evidence.md`（新規）: 5点圧縮証跡（①スコープ・対象外・リスク区分 ②AC対応表 ③機械検証要約＋生ログ参照 ④finding対応・既知制約 ⑤ADR・公開API・互換性影響〔なしの明記含む〕）
- [ ] 🔬 **機械検証**: `bash scripts/doc-check.sh` → green（DOC-MAP 整合・AC7）＋ `npm run test && npm run typecheck && npm run lint` 最終 green（AC6）
- [ ] Codexレビュー自動実行（依頼書 `doc/DD/DD-011/codex-review-request.md` 生成 → `bash scripts/codex-review.sh --request ... --out doc/DD/DD-011/codex-review-result.md` ・effort high・全Phase差分一括）
- [ ] Codexレビュー指摘への対応、または見送り理由をログに記録
- [ ] 😈 **DA批判レビュー**（証跡5点が DD-018 の S1-1/S1-3 判定材料として使える形か）

## ログ

### 2026-07-13
- DD作成（roadmap §3 DD-011 定義・DD-009 `package-boundary.md` §4 / `poc-asset-ledger.md`・DD-010 完了〔CG-2 解除〕を前提に起票）
- Codex 利用可否: **利用可**（`bash scripts/codex-review.sh --check` exit 0・codex-cli 0.144.0-alpha.4）
- 要確認: ①DD差分テンプレの実ファイル改修可否（DD-007 要確認3。既定案=dd-update 非管理の場所へ新設）／②boundary lint 既存違反（R4/R5）の是正範囲（既定案=本DDで是正）／③consumer harness は雛形か実在アプリか（既定案=雛形・実在アプリは DD-016）／④package rename の要否・時期（既定案=既存は見送り・新設 Facade のみ `@nanairo-sheet/*` 直採用。実施なら子DD `DD-011-1`）
- 注記: DD-009 台帳の rename/codec 移設割当のうち、codec 移設は本DDで実施・rename は要確認④の判断に従う（見送り時は台帳との差分をログへ残し DD-016 で再判断）

### 2026-07-13（要確認①〜④確定・オーケストレータ経由のユーザー回答）
- **要確認① = 別ファイル新設で確定**: `doc/plan/dd-risk-class-header.md`（dd-update.sh 上書き対象外）へ Risk Class ヘッダ雛形＋製品化6観点を設置。templates 直接改修はしない（DD-008 先例整合）。
- **要確認② = 確定**: 機械的修正で済む既存違反（R4/R5）は本DDで是正。大きい構造変更が要るものは baseline 化して担当DDへ送る。
- **要確認③ = 雛形で確定**: pack 経由の独立プロジェクト。実在アプリ統合（S1-3 本実証）は DD-016。
- **要確認④ = 「今 rename する」で確定・子DD `DD-011-1_packageリネーム.md` へ切り出し済**。前提訂正: scope 付与は D-003 実施済みのため実体は `@nanairo-sheet/sheet-*` → `@nanairo-sheet/{types,core,collab,server,formula}` のプレフィックス除去＋**ディレクトリ名統一**（`packages/sheet-core`→`packages/core` 等）。**依存: DD-011-1 → DD-011**（本DDは rename 後の最終名の上に構築・論理名↔現行名マップ不要）。本文の該当箇所（背景・決定事項・Phase 1/2 タスク・AC7）を確定内容へ更新。

---

## DA批判レビュー記録

### Phase N DA批判レビュー

**DA観点:** （このPhaseで最も壊れやすいポイントは何か？）

| # | 発見した問題/改善点 | 重要度 | 再現手順（高/中は必須） | DA観点 | 対応 |
|---|-------------------|--------|----------------------|--------|------|
| 1 | (具体的に記述) | 高/中/低 | (高/中: 操作→結果) | (どのDA観点で発見したか) | ✅修正済/⏭️別DD/❌不要 |
