# DD-006 ChatGPTレビュー記録（2026-07-12）

- **レビュアー**: ChatGPT（手動運用方針: dd-autoに組み込まず既存ゲートで実施し結果をDDへ還流）
- **対象**: DD-006 起票直後ドラフト（要確認1〜5未回答の状態）
- **反映コミット**: f396476（DD-006〜007: 起票・レビュー反映）
- **総評**: 「設計は良い。6点を補強してから着手」

## 指摘と採否

| # | 指摘 | 採否 | 反映先 |
|---|------|------|--------|
| 1 | CellStore候補数が「3方式」表記と実体4候補で不一致。4候補を比較し3カテゴリ（Map型／チャンク型2実装／列指向配列型）で総括すべき | ✅採用 | 検討内容・AC1・Phase 1タスク（ファイルを `chunked-column-store` / `chunked-rowslot-store` に分離） |
| 2 | Nodeだけで最終合否を判定しない。Node=相対比較の主評価、採用候補はChrome/Edgeで最小ブラウザ確認（playground非変更なら `apps/pocd-browser-bench` で可） | ✅採用 | 要確認3回答・検討内容・AC9新設・Phase 5タスク（§18.6 No-Go条件「ブラウザーメモリ上限超過」の直接確認と位置づけ） |
| 3 | 再計算性能は「影響式数」を明記。通常入力（影響100式以下）をp95 16msの合否対象にし、影響1,000式／全10,000式／10,000行範囲SUM／10,000式チェーンは実測からWorker分離条件を決める素材にする | ✅採用 | 要確認1回答・AC2・Phase 3 bench-recalcタスク |
| 4 | 固定ID参照の検証がモックAxisViewのみだと実際のInsertRows/DeleteRowsとの食い違いを見逃す。pocd-bench側にsheet-core実文書の結合試験を1本追加 | ✅採用 | 検討内容・AC3/4検証方法・Phase 3 `integration-sheetcore.test.ts` タスク |
| 5 | parserの資源制限（最大式長・ASTノード数・括弧ネスト・関数引数数・範囲セル数・タイムアウトまたは処理量上限）を受け入れ基準へ明示 | ✅採用 | AC8新設・Phase 0テスト設計・Phase 2 `limits.ts`・Phase 3評価器タスク。タイムアウトは同期評価器のため処理量カウンタで代替 |
| 6 | snapshot閾値判断にはreplay時間以外（serialize時間・JSONサイズ・parse/restore時間・メモリ・圧縮時間）も必要 | ⚠️位置づけ調整のうえ採用 | Phase 4 snapshot参考計測タスク・AC5。正式snapshot形式が未設計のため素朴JSON化の**参考計測**（合否対象外）とし、圧縮は任意 |

## 要確認への推奨回答（すべてユーザー確定 2026-07-12）

| 要確認 | 回答 |
|--------|------|
| 1. 再計算SLO | 影響式数別に分割（合否=影響100式以下 p95 16ms/worst 33ms） |
| 2. 関数範囲 | 5関数（SUM/AVERAGE/MIN/MAX/COUNT）まで実装 |
| 3. 計測環境 | Node主評価を承認＋採用候補はブラウザ最小確認 |
| 4. ADR-011 Accepted化 | DD-007で実施 |
| 5. エラー値6種 | 承認 |

## 未対応・注記

- 指摘2/4/6はスコープ増のため、実装中に1レビューサイクル超過の兆候が出たらPhase 4（replay/snapshot計測）を別DDへ分割する（DD本文ログに記録済み）。
