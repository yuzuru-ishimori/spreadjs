# DD-006 ChatGPTレビュー記録（2026-07-12・第2回）

- **レビュアー**: ChatGPT（手動運用方針: dd-autoに組み込まず既存ゲートで実施し結果をDDへ還流）
- **対象**: DD-006 Phase 0 事前精査後のドラフト（`scenarios.md`・`function-spec.md` 先行作成済み・実装はDD-005完了後まで停止中）
- **文脈**: DD-005 を別セッションで実施中の待ち時間に、DD-006・007 の「骨格は十分・全面改稿不要」を前提とした補強助言。DD-006 は「正しく測る仕組み」を先に固める狙い。
- **反映方針**: すべて doc-only（コード・workspace・`package-lock.json` 不変）。DD-005 と無干渉。第1回記録は `chatgpt-review-20260712.md`。
- **総評**: 「骨格は十分。待ち時間に評価方法を結果より先に固定せよ」

## 指摘と採否

| # | 指摘 | 採否 | 反映先 |
|---|------|------|--------|
| 1 | **ベンチ規約を結果を見る前に固定**（ウォームアップ回数・本計測回数・中央値/p95/worstの採用・GCタイミング・4方式の実行順ローテーション・端末/Node/Chrome/Edge版数・生JSON保存先・同一seed再実行）。Node↔ブラウザ「極端な乖離」を定性でなく事前規則化（例: 時間2倍超 or メモリ1.5倍超で原因分析必須）。倍率は詳細設計で決めてよいが結果を見てから変えない | ✅採用 | `DD-006/bench-protocol.md` 新設（§2手続き・§3生JSONスキーマ・§5乖離判定規則）。決定事項・Phase 1 bench-cellstoreタスク・Phase 0 🧪行 |
| 2 | **CellStoreの単一の勝者を無理に決めない**。疎/密/範囲走査/更新で有利構造が異なりうる。ADR-011は「チャンク方式全面採用」でなく用途別選択表でもよい | ✅採用 | 決定事項・AC1文言（用途別選択表を許容）・ADR-011拡充タスク・bench-protocol §6.1 |
| 3 | **データ分布を3種類以上に**（一様な疎／連続した密／上部・左側集中の業務入力型／列ごとに文字列・数値が偏る）。分布で優劣が反転するリスクはDA既記 | ✅採用 | bench-protocol §4（4分布）・scenarios §7/§8・Phase 1 data-genタスク・AC1 |
| 4 | **関数数より意味論を優先**（空白0扱い/文字列無視or#VALUE!/エラー伝播/範囲と直接引数で型変換を変えるか/`-2^2`/NaN・Infinity・負の0/ロケール不変）。function-spec.mdを唯一の正としテスト側に別仕様を作らない | ✅採用（大半は既記・不足分を追加） | 既に `function-spec.md` §2〜§4・`scenarios.md` §2/§6 に空白/文字列/エラー/範囲vs直接/`-2^2`=4 を明記済み。**不足していた NaN/Infinity/負の0**を §2.1、**ロケール不変**を §2.2 として追加。「function-spec単一正・テスト側に別仕様を作らない」を評価器タスク・決定事項へ明記 |
| 5 | **Worker導入条件を成果物にする**。影響式数別の実測から、数値を残すだけでなく判断表（影響式数≤N=同期／>N=Worker候補／巨大範囲=interval必須／深いチェーン=分割orWorker）を出す | ✅採用 | bench-protocol §6.2（判断表テンプレート）・Phase 5 measurement-reportタスク（結論表）。AC2の影響式数別実測は既記 |
| 6 | **snapshot閾値を素朴JSONだけで確定しない**。serialize/parse計測は桁感としては有効だが正式snapshot形式ではない。結論は「本番間隔を確定」でなく「Phase 1で正式形式を設計するための暫定推奨値」に留める | ✅採用 | AC5文言（暫定推奨・確定しない）・bench-protocol §6.3・決定事項。第1回指摘6（参考計測・合否対象外）と整合 |
| 7 | **sheet-formulaにも環境非依存ゲート**。DD-005 sheet-collaborationと同様、テスト型定義からNode型が混入するとDOM/Node依存の誤混入を検出できない。通常typecheck＋`typecheck:core`（テスト除外・types:[]・DOMなし・Nodeなし） | ✅採用 | function-spec §0・Phase 2 `tsconfig.core.json`＋`typecheck:core`タスク・scenarios §6（probe実効性確認）・決定事項 |

## 「今やるべきこと」への準拠

助言の「DD-006は次までで止める」= scenarios.md／function-spec.md／**ベンチ規約案**／**証跡JSONスキーマ案**／Phase 1詳細設計の論点整理——に対応:

- ✅ `scenarios.md`・`function-spec.md`（第1回で作成済み・本回で §2.1/§2.2 追記）
- ✅ ベンチ規約案・証跡JSONスキーマ案 = `bench-protocol.md`（§2・§3）
- ✅ Phase 1詳細設計の論点整理 = `bench-protocol.md` §7（確定チェックリスト）
- ⛔ ワークスペース追加・`package-lock.json` 更新・実装開始 = **DD-005完了後**（未着手を厳守）

## 注記

- 上限値 L1〜L6・乖離倍率（2.0/1.5）・Worker閾値 N・分布パラメータは Phase 1 実装前詳細化で確定し、**確定後は結果を見てから変えない**（後付け基準変更の禁止＝助言 #1 の核心）。
- 指摘 #4 の意味論は第1回時点で大半が明文化済みだったため、本回は不足分（非有限・負の0・ロケール不変）と「単一の正」原則の追補に留めた（全面改稿はしない＝助言前提）。
