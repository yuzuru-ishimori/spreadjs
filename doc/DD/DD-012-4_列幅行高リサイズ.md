# DD-012-4: 列幅・行高リサイズ（ヘッダー境界ドラッグ・view-local＋利用側保存API）

| 作成日 | 更新日 | ステータス | 補足 |
|--------|--------|-----------|------|
| 2026-07-15 | 2026-07-15 | 確認待ち | 実装・Codexレビュー（P1×1/P2×8 全反映）完了・全検証 green。残=実機確認（ユーザー・showcase scenario=resize／CG-6 headed p95） |

```text
Risk Class: B
Risk Triggers: なし（protocol/永続化/IME状態機械 無改変。公開APIは Experimental 0.x への追加のみ）
Human Spec Gate: passed（2026-07-15 AskUserQuestion で保存範囲=ローカル＋利用側保存を確定）
Codex: high（公開API追加＋pointer interaction＋描画座標系）
Manual Gate: あり（実機ドラッグ操作の確認＝ユーザー・showcase デモで）＋perf smoke（scroll p95 予算・§2.3）
External Review: なし
Evidence Level: standard
```

> アプローチ: E2E駆動（操作→結果の検証が中心）
> 親: DD-012（単一利用者縦切り）。発見元=DD-017-2 showcase 実機確認のユーザー要望（2026-07-15）
> 兄弟依存: **DD-012-5（オーバーフロー・折り返し）が本DDの行高 override 機構に依存**。本DDを先に完了させる

## 目的

Excel 同様に列ヘッダー・行ヘッダーの境界をドラッグして列幅・行高を変更できるようにする。設定は **view-local**（他ユーザーへ即時同期しない）とし、変更イベント＋mount 初期値の公開APIで**利用側アプリが保存・復元**できるようにする（保存先を共有にすれば F5 リロードで他ユーザーへも反映＝ユーザー確認済みの要件）。

## 決定事項（仕様確定済み）

- **D1 保存範囲 = view-local＋利用側保存API**（ユーザー確定 2026-07-15）。protocol・snapshot・Operation は無改変。全ユーザーリアルタイム共有は Stage 2 バックログへ（stage2-backlog.md に記録済み）。
- **D2 公開API（Experimental 0.x 追加・CHANGELOG 必記）**:
  - `GridMountOptions.columnWidths?: Readonly<Record<string, number>>`（ColumnId→px・初期 override）
  - `GridMountOptions.rowHeights?: Readonly<Record<string, number>>`（RowId→px・初期 override）
  - `GridEvent` に `{ type: 'layout'; columnWidths: Record<string, number>; rowHeights: Record<string, number> }` を追加。**ドラッグ確定時（pointerup）に発火**・内容は override のみ（既定値の列/行は含まない）。利用側はこれを保存し、次回 mount の初期値に渡す（F5 反映の実現手段）
- **D3 操作仕様（Excel 風）**: 列ヘッダー右端／行ヘッダー下端の **±4px** でカーソルを `col-resize`/`row-resize` に変更。左ボタンドラッグで変更・ドラッグ中ライブ再描画。最小 列 20px／行 16px・最大 2000px でクランプ。**ダブルクリック auto-fit は対象外**（Stage 2 バックログ記録済み）。
- **D4 実装方式**: 既存資産をそのまま使う — `Axis.setSizeById`（`packages/render/src/axis.ts`・実装済み）＋`ViewportTransform.hitTest`（`column-header`/`row-header` 判別済み）＋`columnHeaderRect`/`rowHeaderRect`。リサイズ後は `markViewportDirty` → 再描画＋（編集中なら）`refreshPlacement` で textarea 追従。
- **D5 IME 不変の維持**: composition 中のリサイズも textarea の value/selection/DOM 親には触れない（`place()` のみ＝I-3 の許容範囲・既存 refreshPlacement 経路）。リサイズ操作は編集状態機械に**イベントを流さない**（pointerdown をリサイズ層で先取りし editor へ渡さない）。
- **D6 features.json 更新**: 完了時に `apps/showcase/src/features.json` へ「列幅・行高の変更」を提供中で追加＋デモシナリオ追記（AGENTS.md 義務）。

## 受け入れ基準

| # | 基準（操作 → 期待結果） | 検証方法 |
|---|------------------------|---------|
| 1 | 列ヘッダー境界±4pxで col-resize カーソル→ドラッグで列幅が変わり、セル・ヘッダー・選択枠・編集textareaの位置が追従する | E2E（Playwright・実ドラッグ）＋ユニット（transform/axis） |
| 2 | 行ヘッダー境界ドラッグで行高が変わる（同上の追従） | E2E＋ユニット |
| 3 | ドラッグ確定時に `layout` GridEvent が発火し、override のみを含む | ユニット（mount-controller）＋E2E |
| 4 | `columnWidths`/`rowHeights` を mount 時に渡すと初期表示がその幅・高になる（→保存済み設定のF5反映が成立） | ユニット＋E2E（再mountで復元） |
| 5 | クランプ（列20〜2000px・行16〜2000px）が効く | ユニット |
| 6 | 変換中（composing）にリサイズしても draft/textarea が壊れない（I-3不変） | ユニット（ime-editing-session 連携）＋既存不変条件スイート green |
| 7 | 5万行シードで scroll p95 予算内（§2.3 性能回帰・12-2 計測ハーネス） | perf smoke |
| 8 | showcase features.json 更新＋デモで実演可能・CHANGELOG に API 追加記載 | doc 確認＋e2e:showcase green |

## タスク一覧

### Phase 0: 事前精査（dd-implementer が着手時に実施）
- [x] 📋 タスク精査・📐 詳細化判定（`mount-controller.ts` の pointer 配線・resize 状態機械の設計を確定）
- [x] 🧑‍⚖️ Codex 要否 = **必須・high**（公開API追加・interaction）
- [x] 😈 DA調査（ドラッグ中の scroll 併発・frozen 列境界・DPR・pointer capture 漏れ）

### Phase 1: リサイズ interaction＋公開API
- [x] `packages/grid/src/mount-controller.ts`: ヘッダー境界 hit（±4px）判定・cursor 切替・pointerdown/move/up（setPointerCapture）で `Axis.setSizeById` 更新→ `markViewportDirty`＋`refreshPlacement`。editor へイベントを流さない（D5）
- [x] `packages/grid/src/index.ts`: `GridMountOptions.columnWidths/rowHeights`・`GridEvent 'layout'` 追加（D2。内部型を漏らさない=R7）
- [x] `packages/grid/src/document-view.ts` ほか: 初期 override を Axis へ注入（`AxisConfig.overrides` 実装済みを使用）。**構造再構築（列順変更等）時に override が失われない**ことをテストで固定
- [x] 🔬 機械検証（ユニット）: hit判定境界値・クランプ・layout イベント内容・初期値注入・override 再構築維持
- [x] 🔬 機械検証（E2E・`apps/playground/e2e/` へ追加）: 実ドラッグ→列幅変化→再mount 復元（AC1〜4）
- [x] 😈 DA批判レビュー
- [x] Codexレビュー（high）＋反映

### Phase 2: 検証・運用固定
- [x] 🔬 機械検証（全体）: `npm test`・`test:invariants`・`test:e2e`・`typecheck`/`lint(boundary)` green・perf smoke（AC7）
- [x] `CHANGELOG.md`: Experimental API 追加を記載／`apps/showcase/src/features.json`＋`scenarios.ts` へ「列幅・行高の変更」追加（D6）・`test:e2e:showcase` green
- [x] 😈 DA批判レビュー
- [ ] 実機確認（ユーザー・showcase デモ）＝**ユーザー担当**（実ドラッグ操作＋F5 復元の目視・実 Chrome perp CG-6 headed 計測）

## ログ

### 2026-07-15
- 起票。仕様3点をユーザー確定（保存範囲=ローカル＋利用側保存API／F5反映=初期値注入で実現・共有化は Stage 2）。Axis の Id単位 override・hitTest のヘッダー判別が実装済みであることを確認済み

### 2026-07-15（実装・dd-implementer / Opus）
- **実装完了**。変更ファイル:
  - 新規 `packages/render`（無改変・既存 Axis/viewport をそのまま利用）
  - 新規 `packages/grid/src/resize-interaction.ts`（純粋: 境界 hit 判定・クランプ・サイズ算出）＋ `resize-interaction.test.ts`
  - `packages/grid/src/document-view.ts`（初期 override 注入・クランプ・構造再構築維持・既定戻しで解除・record 出力）＋ `document-view.test.ts`
  - `packages/grid/src/mount-controller.ts`（pointer 配線・drag 状態機械・cursor・layout emit・spacer 同期・debug API）
  - `packages/grid/src/session-sync.ts`・`index.ts`（`GridMountOptions.columnWidths/rowHeights`・`GridEvent 'layout'` 追加＝D2）・`internal.ts`（E2E introspection）
  - consumer: `apps/playground/src/integration/main.ts`・`apps/showcase/src/demo/main.ts`（localStorage 保存/復元＝D1/D2 の利用側保存の実演）
  - `apps/playground/e2e/resize.spec.ts`＋helpers（実ドラッグ E2E・AC1〜4）
  - `CHANGELOG.md`（Unreleased・Experimental API 追加）・`apps/showcase/src/features.json`＋`scenarios.ts`（「列幅・行高の変更」＝D6）
- **検証（全 green）**: typecheck / lint（boundary new=0）/ `npm test`=766 pass / `test:invariants`=40 pass / e2e(playground)=11 pass / e2e(showcase)=3 pass。
- **perf smoke（AC7）**: スクロールのホットパスは本DDで無改変（override は resize ドラッグ時のみ更新・スクロール中は pointermove 非発火・`Axis.size` の Map 参照は従来から存在）。perf 予算 tripwire（`tests/invariants/perf`・`scripts/cg-perf/perf-budget.json` scrollFrameP95Ms=16.8）green。実 Chrome headed CG-6 の p95 実測はユーザー Manual Gate に残す。
- **Codexレビュー（必須・high）**: `doc/DD/DD-012-4/codex-review-result.md`（P1×1・P2×8）。**全9件対応**:
  1. [P1] リサイズで spacer 未同期→末尾までスクロール不可 → 各 pointermove で `syncSpacer()` を呼ぶ（cancel 復元時も）。
  2. [P2] 初期 override 無検証 → `DocumentView` 取り込み時に有限数へ絞り列20-2000/行16-2000へクランプ。
  3. [P2] 末尾要素外の空白帯を誤ってリサイズ対象化 → hit 判定を localX ベースから境界線距離（abs<=handle）へ変更し空白帯を除外。
  4. [P2] 既定へ戻しても override が残る → `setColumn/RowWidth/Height` で既定一致時に override を解除（layout は override のみ）。
  5. [P2] pointercancel でも layout 確定発火 → `finishResize(emit)` に分離。cancel/capture 喪失は非確定で開始時サイズへ復元。
  6. [P2] frozen 境界越えの左端掴みで別列を誤検出 → 左端分岐に adjacency ガード（前列の右端が現列左端に一致する時のみ）。
  7. [P2] 非 active pointer の move で誤リサイズ／ドラッグ中の新規 pointerdown → pointerId 一致ガード＋ドラッグ中 pointerdown 無視。
  8. [P2] 対象を index 保持で構造Op に脆弱 → Column/RowId で保持し毎 move で index 再解決。
  9. [P2] スクロールで origin 陳腐化 → origin を毎 move で現在 transform から再計算。
  - findings は全て実害ある境界ケースと判断し全反映（見送りなし）。
- **残: 実機確認（ユーザー）**＝実 Chrome での実ドラッグ操作感・F5 復元の目視・CG-6 headed p95 実測（showcase デモ scenario=resize）。

---

## DA批判レビュー記録

### Phase 1/2 DA批判レビュー（実装者・2026-07-15）

**DA観点:** pointer 状態機械のリーク・IME 不変の破れ・override の消失・空/境界データでの例外。

| # | 発見した問題/改善点 | 重要度 | 再現手順（高/中は必須） | DA観点 | 対応 |
|---|-------------------|--------|----------------------|--------|------|
| 1 | pointerup と lostpointercapture で `layout` が二重発火しうる | 中 | ドラッグ確定→pointerup が releasePointerCapture を呼ぶと lostpointercapture も発火 | 二重通知で利用側保存が余計に走る | `endResize` で `resizeDrag=null` を release より先に実行し、pointerId 一致ガードで 2 度目を no-op 化。E2E で override が 1 列のみを確認 |
| 2 | 空 Axis（行0件）でヘッダー境界 hit すると `getId` が throw | 中 | 行0件の状態で行ヘッダー帯にホバー | 例外で描画/操作が落ちる | `resizeHitTest` に rowCount/colCount ガード追加＋ユニットで固定 |
| 3 | 構造Op（行挿入/削除）で Axis 再構築時にリサイズ済み override が消える | 高 | 列/行をリサイズ→行挿入 | AC4 の復元不変が崩れる | `DocumentView` が override を別途保持し `createAxis({overrides})` へ毎回注入。ユニットで再構築後も維持を固定 |
| 4 | リサイズ pointer が編集状態機械へ流れると IME/選択が乱れる（D5 破れ） | 高 | 変換中にヘッダー境界をドラッグ | I-3 不変違反 | pointerdown でリサイズを先取りし `editor.pointerdownCell` を呼ばず return。再描画は `refreshPlacement`＝`place()` のみ（value/selection 不触） |
| 5 | 境界共有（列 c 右端＝列 c+1 左端）でどちらの列がリサイズ対象か曖昧 | 低 | 境界ぴったりにホバー | 対象列の取り違え | 右端 handle 優先・次列左端 handle は前列に写像。ユニットで両側を固定 |
| 6 | ドラッグ中のスクロール併発で origin がずれる | 低 | ドラッグ中にホイール | サイズが飛ぶ | `setPointerCapture` でスクロールへ流さず、対象列/行の左端/上端は自サイズ変更で不動ゆえ origin 固定で正しい |
