# DD-033: 明細閲覧ビュー（表示専用モード・列見出しキャプション・数値/日付表示書式）

| 作成日 | 更新日 | ステータス | 補足 |
|--------|--------|-----------|------|
| 2026-07-21 | 2026-07-21 | 保留 | 起票のみ完了。再開条件=DD-027 完了（ユーザー指示）。要確認②〜⑥は実行開始時に最終確定 |

```text
Risk Class: A（公開 API 新設〔mount オプション3点・操作仕様の新設〕・編集抑止が確定/送信経路に接する）
Risk Triggers: 公開 API/Options の新設（readOnly・columnCaptions・columnDisplayFormats）／readOnly が編集入口＋chokepoint（submitSetCells・行操作 submit・Undo/Redo）へ抑止層を追加（editor-state-machine 本体は無改変が設計目標＝改変が必要と判明したら停止して T1 昇格）／描画フレーム予算（キャプション・書式済みテキストの描画コスト）／受け入れ基準・操作仕様の新設
Human Spec Gate: required（要確認①〜⑥をユーザー確定後に子DD起票。DD-027 前例）
Codex: 不可（Codex未導入・本起票環境 `bash scripts/codex-review.sh --check` exit 1）→ Fable 5 レビューで代替（前例=DD-021/027 のユーザー決定済み運用）。各子DD 必須相当・effort high 相当。実装セッション開始時に --check を再実行し、利用可なら Codex high へ戻す
Manual Gate: あり（M1 readOnly 実機スモーク・M2 キャプション/書式/50k行スクロール体感。editor-state-machine/常駐 textarea に触れた場合のみ T1=ime-manual-gate-ledger §2 の5点＋変更固有手順・Claude 代行可）
External Review: なし（Phase 境界・ADR 転換・Go/No-Go に非該当）
Evidence Level: full（A区分。描画性能の再現コマンド・既知の未保証境界を省略しない）
```

> アプローチ: 親=標準（アンブレラ管理・統合検証）。子DDで混在: DD-033-1=E2E駆動＋TDD（抑止の網羅検証）／DD-033-2=TDD＋E2E（書式純関数＋描画）。軽量DD判定: 非該当（新規公開 API・src 3ファイル超・子DD分割あり）。

## 目的

BI・帳票系アプリの「明細レコード閲覧」を SDK の1構成で成立させる。**表示専用モード（readOnly）・列見出しキャプション・数値/日付表示書式**の3点により、「データと列スキーマを渡すだけで業務の表として読める」状態を提供する（既存の columnWidths / wrapColumns / columnTypes / columnFormats に連なる列スキーマの完成）。

## 背景・課題

- 駆動要因: 実案件（BIツール開発）で**明細レコードの閲覧ニーズが高い**ことを確認（2026-07-21・ユーザー）。「Excelにデータ表示したい」の実体は (a) Web 画面での Excel ライク閲覧 と (b) xlsx 持ち出し。本DDは (a) を提供する。(b) Excel I/O は計画書 §2.5 の対象外のまま（範囲コピー TSV→Excel 貼り付け=DD-020 提供済みが代替。スタイル付き xlsx 生成はグリッド外の別コンポーネント候補として、要求が確定したら別DD）。
- 既存実装の実調査（2026-07-21・起票時）:
  - **公開面（先例あり）**: `packages/grid/src/index.ts` の `GridCommonMountOptions` に列単位設定の運用が確立済み（columnWidths/rowHeights=DD-012-4・wrapColumns=DD-012-5・columnTypes=DD-027-1/2・columnFormats=DD-027-3）。mount 時固定・両モード共通・不正設定は mount fail-fast（code=`column-types-invalid`）。
  - **列ヘッダー（キャプション未実装）**: `packages/render/src/base-layer.ts` の `columnLabel()` が A/B/C… を固定描画するのみ（129・464 行）。業務名を出す機構は無い。列ヘッダー自体は Canvas チュームとして常時固定描画＝BI 要件「ヘッダー行固定」は既存構造で充足済み。
  - **数値・日付表示（書式未実装）**: `packages/grid/src/document-view.ts` の `cellScalarToDisplay` による素の文字列化のみ。千位区切り・小数桁・%・日付パターンは無い。
  - **表示専用（未実装）**: `document-view.ts` の内部イミュータブルガード（set/bulkLoad throw）のみで、製品機能としての readOnly は無い。文書変更の入口は `mount-controller.ts` の pointerdown/dblclick/キー入力・paste/cut（DD-020）・行操作ショートカット（DD-021）・Undo/Redo。確定系 chokepoint は `submitSetCells`（DD-020）＋行操作 submit（DD-021）。
  - **契約面**: 表示文字列は cell-commit/setData round-trip・コピー TSV・columnFormats 完全一致（DD-027 決定⑥）が依存する契約面。表示書式がこれを変えるかは要確認⑤。
- 位置づけ: Stage 2 ロードマップ（`doc/plan/phase2-dd-roadmap.md` §1）の採番 DD-023〜032 に無い consumer 駆動の新規要望。roadmap 注記は「想定外DDは子DDで起票」とするが、本件は既存DDの子として不自然な独立機能のため新規トップレベル DD-033 で起票し、ロードマップ上の扱いを要確認①でユーザー確定する。

## 検討内容

### D1 子DD分割（レビューゲート2回＝親子分割・guides §6）

| 子DD | スコープ | アプローチ | 主対象（見込み） |
|---|---|---|---|
| DD-033-1 表示専用モード | `readOnly` mount オプション・編集入口＋chokepoint の2層抑止・閲覧系操作の維持・共同編集モードでの受信専用化 | E2E駆動＋TDD | `packages/grid/src/index.ts`・`mount-controller.ts`・`standalone-session.ts`/`session-sync.ts`・`apps/playground/e2e` |
| DD-033-2 列見出しキャプション・表示書式 | `columnCaptions`・`columnDisplayFormats`（数値・日付）・書式純関数・ヘッダー/セル描画接続・性能 | TDD＋E2E | `packages/grid/src/index.ts`・`document-view.ts`・`packages/render/src/base-layer.ts`・新規書式モジュール |

実装順: 033-1 → 033-2（モード基盤→表示拡張）。統合検証・デモ・提供開始は親 Phase 3 に集約。子DD本文は**要確認①〜⑥の確定後に起票**（DD-020/021/027 先例・確定前仕様の固定化を避ける）。

### D2〜D7（要確認①〜⑥の根拠）

- **D2（①位置づけ）**: S2-1 は既定 consumer 2件で充足予定・roadmap §4 R5 は「3件目 candidate 探索はスコープ外（推測で計画しない）」だが、本件は推測でなく確認済みの実案件ニーズ。**機能提供（本DD）と consumer 統合（BI案件への組み込み）を分離**すれば R5 と衝突せずに前倒しできる。
- **D3（②抑止の層）**: 入口抑止（編集 UI を開かない）だけでは paste・ショートカット等の取りこぼしが構造的に残り、chokepoint 抑止だけでは「編集できたのに確定で失敗する」不自然 UX になる。**両層**（入口=UX・chokepoint=保証）で防ぐ。editor-state-machine 無改変（mount-controller 側の分岐のみ）が設計目標。
- **D4（③キャプション）**: A/B/C の置換描画が最小実装。行番号ヘッダーの非表示化・2段ヘッダーは要求未確認のため対象外。
- **D5（④書式語彙）**: 自前ミニ書式（決定的・依存ゼロ・テスト容易）／Intl 委譲（実装小・ロケール/環境差）／Excel 書式文字列（互換期待の無限拡大=リスク R-15）の3案から、既定=自前ミニ書式。
- **D6（⑤適用面）**: 表示文字列は契約面（背景・課題）のため、書式は **Canvas 描画テキストへのみ**適用が安全。コピーが raw のままなら Excel 貼り付け後に Excel 側で数値書式を適用できる利点もある。columnFormats（DD-027-3）の完全一致も raw 対象のまま＝既存挙動不変。
- **D7（⑥mount 契約）**: wrapColumns/columnTypes の確立済み運用（mount 時固定・両モード共通・fail-fast）を踏襲し、新概念を増やさない。

## 決定事項

- **本DDは起票のみで一旦停止し、実行（子DD起票→実装）は DD-027 完了後に開始する（ユーザー確定 2026-07-21）**＝要確認①の着手順部分が確定。
- 残りの要確認（①の Stage 2 追記・consumer 統合分離、②〜⑥）は異論提示なし＝既定案を暫定とし、**実行開始時（子DD起票前）に最終確定**する。確定値は各子DD〔033-1: ①②⑥／033-2: ③④⑤⑥〕の決定事項へ転記する。

## 要確認（Human Spec Gate・各項目に既定案）

1. **要確認①: ロードマップ上の位置づけと着手順** — 既定案: 新規トップレベル **DD-033 として Stage 2 に追加**（`phase2-dd-roadmap.md` §1 への追記=更新記録）。着手は **DD-027 完了後**（base-layer・mount オプション・showcase を共に触るため）。BI 案件への consumer 統合は本DD対象外とし、3件目 candidate として DD-032 の Stage 3 バックログ棚卸しへ記録（R5 と非衝突）。対案: Stage 3 送り（BI 案件の時期に間に合わないリスク）。
2. **要確認②: readOnly の抑止範囲** — 既定案: 文書変更系を全抑止（編集開始〔キー入力・F2・ダブルクリック・IME〕・paste/cut・Delete/Backspace クリア・行挿入削除〔ショートカット・公開 API〕・Undo/Redo）。閲覧系は維持（範囲選択・コピー・スクロール・列幅行高リサイズ・link-open）。選択式列のドロップダウンは開かない。共同編集モードでは**受信反映のみ**（自クライアントから文書 Operation 送信ゼロ）。`setData`（プログラム的注入）は**許可**（閲覧データの差し替え手段）。抑止時はサイレントにせず onDiagnostic へ notice を出す。**注意: readOnly は権限制御ではない**（サーバー側強制なし・利用側責務として既知制約へ記録）。
3. **要確認③: 列見出しキャプション** — 既定案: `columnCaptions`（ColumnId 文字列→表示名）。指定列は A/B/C を置換描画・未指定列は従来どおり。行番号ヘッダーは現状維持・2段ヘッダーは対象外。
4. **要確認④: 数値・日付書式の語彙** — 既定案: 自前ミニ書式 `columnDisplayFormats`（ColumnId→書式）。number=`{ grouping?, decimals?, percent?, prefix?, suffix? }`・date=固定トークン（`YYYY`/`MM`/`DD`/`HH`/`mm`/`ss`）のパターン文字列。Excel 書式文字列互換・Intl ロケール書式は v1 対象外（拡張点メモのみ）。
5. **要確認⑤: 書式の適用面** — 既定案: **Canvas 描画テキストのみ**整形。cell-commit/setData round-trip・コピー TSV・columnFormats 完全一致・編集ドラフト文字列は従来の表示文字列（raw）のまま＝**契約不変**。
6. **要確認⑥: mount 契約** — 既定案: 3オプションとも両モード共通・mount 時固定（wrapColumns/columnTypes と同運用）。不正設定（未知列・不正パターン・decimals 範囲外等）は mount 時 fail-fast（phase=config・新 error code は子DDで確定〔`column-types-invalid` と同経路〕）。共同編集モードでの全クライアント設定一致は利用側責務。

## 受け入れ基準

| # | 基準（操作 → 期待結果） | 検証方法 |
|---|------------------------|---------|
| 1 | readOnly 指定で、キー入力・F2/ダブルクリック・paste/cut・Delete/Backspace・行挿入削除（ショートカット・API）・Undo/Redo のいずれでも文書が変更されない（cell-commit 0件・文書 Operation 送信 0件・診断 notice あり） | DD-033-1 unit＋E2E |
| 2 | readOnly でも範囲選択・コピー（TSV 内容が従来と同一）・スクロール・列幅行高リサイズ・link-open が従来どおり動く | DD-033-1 E2E |
| 3 | readOnly（共同編集モード）でリモート更新が画面へ反映され、自クライアントから文書 Operation が送信されない | DD-033-1 unit（collab 経路） |
| 4 | columnCaptions 指定列のヘッダーが指定文言で描画され、未指定列は A/B/C のまま | DD-033-2 unit＋E2E |
| 5 | 数値書式（grouping/decimals/percent/prefix/suffix）・日付書式（トークンパターン）が描画に適用され、コピー TSV・cell-commit の値は書式適用前と同一（raw） | DD-033-2 unit＋E2E |
| 6 | 3オプションいずれも未指定の既存 consumer は現行挙動と完全一致 | 親 Phase 3 全回帰（unit・invariants・playground E2E 全スイート） |
| 7 | 50k 行の明細ビューデモ（readOnly＋キャプション＋書式＋columnFormats 併用）でスクロール性能予算内（p95 16.7ms・redraw 12ms・§2.3 ガードレール） | 親 Phase 3 headed 計測（run-cg6 ハーネス before/after） |
| 8 | 公開 API/イベント差分が CHANGELOG・error-codes.md・API 型 snapshot に記録される | 親 Phase 3 網羅確認＋contract test |
| 9 | features.json へ明細閲覧ビューのエントリ＋showcase デモシナリオが追加される | 親 Phase 3 features smoke＋showcase E2E |
| 10 | 要確認①の確定内容（roadmap 追記または DD-032 バックログ記録）が文書へ反映される | 親 Phase 3 doc-check |

## タスク一覧

### Phase 0: 事前精査
- [x] ⚖️ 軽量DD判定 → 非該当（新規公開 API・src 3ファイル超・子DD分割あり）
- [ ] 📋 各Phaseのタスク精査・詳細化（子DD起票時に再確認: AC⇔検証対応・対象パス・🔬の有無）
- [ ] 📐 実装前詳細化トリガー判定（判定結果: DD-033-1 → **要**〔公開 I/F 新設・既存編集入口の分岐変更・collab 送信経路〕／DD-033-2 → **要**〔描画性能特性・3ファイル以上・表示文字列契約の確認〕。各子DD冒頭の 📐 で実施・ユーザーレビュー込み）
- [ ] 🧑‍⚖️ Codexレビュー要否判定（判定結果: 本起票環境=**不可（Codex未導入・`--check` exit 1）** → **Fable 5 レビューで代替**〔DD-021/027 前例〕: 各子DD 必須相当・effort high 相当〔公開 API 新設・編集抑止の網羅性・描画変更〕・親 Phase 3 は不要〔子で実施済み〕。実装セッションで --check 再実行し、利用可なら Codex high へ戻す）
- [ ] 😈 Devil's Advocate調査（抑止の取りこぼし構造〔今後編集入口が増えるたびに漏れる→chokepoint 側ガードで恒久化できるか〕／表示書式と columnFormats 完全一致の「見た目と一致条件のズレ」混乱／columnCaptions と columnOrder の不一致設定／50k 行×書式計算の描画コスト／readOnly でも resize 可を「編集できる」と誤解する UX／readOnly を権限制御と誤解するリスク）

### Phase 1: DD-033-1 表示専用モード（実装は子DD参照）
- [ ] 子DD起票: `doc/DD/DD-033-1_表示専用モード.md`（要確認②⑥の確定値転記・📐 実施〔抑止2層の接続点特定=`mount-controller.ts` 入口＋`submitSetCells`/行操作 submit 手前・editor-state-machine 無改変の確認=改変が必要なら停止して T1 昇格〕）
- [ ] DD-033-1 の全Phase完了確認（Fable 5 レビュー込み）
- [ ] 🔬 機械検証: `npm run test && npm run typecheck && npm run lint` → 全 green（boundary new=0）

### Phase 2: DD-033-2 列見出しキャプション・表示書式（実装は子DD参照）
- [ ] 子DD起票: `doc/DD/DD-033-2_列見出し表示書式.md`（要確認③④⑤⑥の確定値転記・📐 実施〔書式純関数の置き場・`base-layer.ts` 接続点・text-cache との整合・mount 時プリコンパイル=DD-027-3 前例〕）
- [ ] DD-033-2 の全Phase完了確認（Fable 5 レビュー込み）
- [ ] 🔬 機械検証: `npm run test && npx playwright test`（対象 spec）→ green

### Phase 3: 統合検証・提供開始（本DDで実施）
- [ ] 統合回帰（1回）: `npm run test`＋`npm run test:invariants`＋`npm run lint`＋`npm run typecheck`＋playground E2E 全スイート＋showcase → 全 green（AC6。view 状態読取は expect.poll・大量変更 spec は zz- 隔離=DD-021 教訓）
- [ ] 50k 行明細ビューデモ: `apps/showcase/src/demo/scenarios.ts` へ「明細閲覧ビュー」シナリオ追加＋`apps/showcase/src/features.json` へエントリ追加（AC9・features smoke green）
- [ ] headed 計測: run-cg6 ハーネス before/after（readOnly＋キャプション＋書式＋columnFormats 併用構成）→ 予算内（AC7）
- [ ] `CHANGELOG.md`・`doc/DD/DD-017/error-codes.md`・API 型 snapshot 差分の網羅確認（AC8）
- [ ] 要確認①の確定反映: `doc/plan/phase2-dd-roadmap.md` への追記（Stage 2 追加の場合）または DD-032 バックログ記録（AC10）
- [ ] 既知制約の記録（readOnly はサーバー側強制の無い表示制御＝権限制御は利用側責務・書式/キャプションは view-local・共同編集での設定一致は利用側責務）
- [ ] 効果測定（DD-034 AC5 移管分）: 本DD系列（親＋子）のコミット数・doc執筆行数を計測し、旧ルール実績（DD-020/021=平均約5コミット・約2,000行/DD・DD-027 実績も比較点に追加）と比較して本DDログへ1行記録。測定対象は記録git一本化・親統合時レビュー1回・完了処理1コミットの効果（本DDは Risk Class A のため軽量DDパス自体は対象外）
- [ ] 🔬 機械検証: `bash scripts/doc-check.sh` → 整合 OK
- [ ] 😈 DA批判レビュー（下記記録欄へ）
- [ ] Manual Gate の受付（M1〜M2。editor-state-machine 等 IME 経路に触れた場合は T1=台帳5点＋変更固有手順を実IME で実施し ime-manual-gate-ledger §3 へ記録・Claude 代行可）

## Manual Gate（synthetic 自動化を最大化・残余のみ）

| # | 項目 | 正味 |
|---|------|------|
| M1 | readOnly 実機スモーク（編集不能・選択/コピー/スクロール/リサイズ可） | 2分 |
| M2 | キャプション・数値/日付書式・50k行スクロールの体感確認 | 2分 |
| T1 | （editor-state-machine に触れた場合のみ）実IME 台帳5点＋変更固有手順（ime-manual-gate-ledger §2→§3） | 5分 |

## ログ

### 2026-07-21
- DD作成（/dd 新規作成フロー）。駆動要因=実案件（BIツール開発）で明細レコードの閲覧ニーズが高いことをユーザーが確認。「スプレッドシートへ書き出すだけでは見た目が悪い」課題への回答として、列スキーマの完成（キャプション・表示書式）＋表示専用モードを1アンブレラに切り出し。
- 番号=DD-033: roadmap §1 で DD-023〜032 が採番済みのため次番号。「想定外DDは子DDで起票」の roadmap 注記に対し、既存DDの子として不自然な独立機能のため新規トップレベルとし、扱いは要確認①でユーザー確定する。
- 既存実装の実調査を「背景・課題」へ記録: readOnly・キャプション・表示書式の3点とも未実装（新規）。再利用資産=列単位 mount オプション運用（DD-012-4/5・DD-027）・確定系 chokepoint（DD-020/021）・書式ルールの mount 時プリコンパイル前例（DD-027-3）。
- 依存関係: DD-027（列タイプ体系）が親 Phase 4 進行中。base-layer・mount オプション・showcase を共に触るため、着手既定=DD-027 完了後（要確認①）。
- Codex 利用可否: 不可（`--check` exit 1）→ Fable 5 レビュー代替（DD-021/027 前例）。実装セッションで --check 再確認。
- Playwright MCP: 本セッション未導入 → エビデンスは常設 Playwright E2E ハーネス（`apps/playground/e2e`）＋手動キャプチャで代替。実装セッションで可否を再確認。
- 仕様確認の応答（ユーザー）: 「起票だけ」＝本DDは起票のみで停止し、実行は DD-027 完了後に開始（要確認①の着手順を確定）。②〜⑥は異論なし＝既定案を暫定固定・実行開始時に最終確定。ステータス 検討中→保留（再開条件=DD-027 完了）。
- DD-034（運用軽量化第2弾）の効果測定タスクを Phase 3 へ受領（新ルール下で最初に完走するDDのため。ユーザー決定）。
- 整合メモ: Phase 1/2 の「各子DD Fable 5 レビュー込み」は DD-034 新ルール（レビューは親統合時1回のみ）より前の起票内容。実行開始時の要確認最終確定にあわせ、親 Phase 3 での1回レビューへ寄せるか Risk Class A を理由に子レビューを維持するかを判断してログへ1行残す。

---

## DA批判レビュー記録

### Phase 3 DA批判レビュー

**DA観点:** （実施時に記載: 抑止漏れの構造リスク・表示書式と契約面の整合・描画予算・readOnly の権限制御誤解）

| # | 発見した問題/改善点 | 重要度 | 再現手順（高/中は必須) | DA観点 | 対応 |
|---|-------------------|--------|----------------------|--------|------|
| | | | | | |
