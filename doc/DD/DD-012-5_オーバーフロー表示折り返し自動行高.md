# DD-012-5: Excel風テキスト表示（隣接空セルへのオーバーフロー・列単位折り返し・自動行高）

| 作成日 | 更新日 | ステータス | 補足 |
|--------|--------|-----------|------|
| 2026-07-15 | 2026-07-15 | 検討中 | 仕様確定済（ユーザー 2026-07-15: 列単位wrap・自動行高=Excel風）。**DD-012-4 完了後に着手**（行高 override 機構に依存） |

```text
Risk Class: B（描画ホットパス変更。データ消失なし・protocol無改変。性能予算超過時は境界化 or 昇格判断）
Risk Triggers: Canvas 描画キャッシュ・可視セル走査の変更＝§2.3 性能フル再計測トリガー
Human Spec Gate: passed（2026-07-15 AskUserQuestion で wrap=列単位・行高=自動拡張を確定）
Codex: high（描画ホットパス・自動行高の再計算トリガー網羅）
Manual Gate: あり（実機で Excel 風挙動の確認＝ユーザー）＋perf フル再計測（scroll p95・redraw 予算）
External Review: なし
Evidence Level: standard
```

> アプローチ: E2E駆動＋TDD（行分割・オーバーフロー走査は純関数でTDD）
> 親: DD-012。発見元=DD-017-2 showcase 実機確認のユーザー要望（2026-07-15）。依存: **DD-012-4（行高 override・layout イベント）完了後**

## 目的

Excel 風のテキスト表示を再現する:
1. **オーバーフロー**: A1 に長い文字列・B1 が空なら、A1 のテキストが B1（以降の連続空セル）へはみ出して表示される。B1 に値が入ればはみ出しは止まりクリップされる。
2. **折り返し**: 列に「折り返す」プロパティが設定されていたら、はみ出さずセル内で折り返す。**折り返しで収まらない行は必要な高さへ自動拡張**（Excel 同様）。

## 決定事項（仕様確定済み）

- **D1 wrap は列単位**（ユーザー確定）: `GridMountOptions.wrapColumns?: readonly string[]`（ColumnId）。セル単位書式モデルは Stage 2（stage2-backlog 記録済み）。実行時切替APIは今回見送り（必要になったら追加・mount option のみ）。
- **D2 オーバーフロー仕様（Excel 準拠・最小）**: 対象=**左寄せ描画の文字列セル**のみ（数値=右寄せは対象外と明記）。右方向の連続空セルへ表示を延長し、非空セルの手前・pane（固定列/ヘッダー）境界・viewport clip で止める。wrap 列のセルはオーバーフローしない（折り返す）。**データは不変・描画のみ**。
- **D3 可視走査の拡張**: 可視範囲の**左外**の非空セルからの流入を描画するため、行ごとに可視開始列から左へ最大 **20 列**遡って直近の非空文字列セルを探索（chunk-store 範囲クエリ）。20 列を超える流入は描画しない（**境界として明記**・Excel too は実質無制限だが性能予算優先。予算に余裕があれば Codex/実測後に拡大可）。
- **D4 折り返しの行分割**: 文字単位（CJK 前提・単語境界なし）。`measureText` キャッシュ（既存 TextMetricsCache）を使い、**値×幅キーの行分割キャッシュ**を新設（LRU・上限件数明記）。
- **D5 自動行高（Excel 風）**: wrap 列に非空値を持つ行の必要高 = max(各 wrap セルの行数)×lineHeight＋padding。再計算トリガー = ①bootstrap 直後の一括計算 ②セル値変更（ローカル楽観適用・リモート適用・rollback/replay 後） ③列幅変更（DD-012-4 の layout 変更） ④wrap 設定は mount 時固定。**手動リサイズ済みの行は手動値を優先**（Excel 同様・DD-012-4 の手動 override と区別するため「自動高」は別レイヤで保持し、手動 override があれば適用しない）。
- **D6 初期一括計算の性能ゲート**: 5万行シード（wrap 列非空 10万セル級）での bootstrap 後一括計算の所要を実測し、**予算（目安 ≦200ms・要実測確定）内なら採用**。超えるなら「可視行＋overscan のみ計算・スクロールで追補（totalSize が漸進更新される制約を境界として明記）」へフォールバック。判断は実測値を DD に記録して行う。
- **D7 自動行高と layout イベント**: 自動高は **`layout` GridEvent に含めない**（利用側が保存すべきは手動 override のみ。自動高は環境・フォントで再現される導出値）。
- **D8 features.json 更新**: 完了時に「Excel風テキスト表示（オーバーフロー・折り返し）」を提供中で追加＋デモシナリオ（長文入力→はみ出し→隣に値→クリップ→wrap列で折り返し・行高自動拡張）。

## 受け入れ基準

| # | 基準（操作 → 期待結果） | 検証方法 |
|---|------------------------|---------|
| 1 | A1 に長文・B1 空 → A1 のテキストが B1 側へはみ出して全文表示（データは A1 のみ） | E2E＋ユニット（走査純関数） |
| 2 | B1 に値を入力 → A1 は B1 の手前でクリップ（fitText 省略記号） | E2E＋ユニット |
| 3 | はみ出し元セルが画面左外でも流入部分が描画される（D3・20列境界内） | ユニット（可視走査） |
| 4 | wrap 指定列は折り返し表示され、はみ出さない | E2E＋ユニット（行分割TDD） |
| 5 | 折り返しで収まらない行は自動で高さが拡張され、値の削除・短縮で自動縮小する。手動リサイズ済み行は手動値優先 | E2E＋ユニット（D5トリガー網羅） |
| 6 | リモート更新（他ユーザーの SetCells）でも自動行高・オーバーフローが追従し、IME 編集中の draft は不変（I-3/#8） | ユニット＋不変条件スイート green |
| 7 | 5万行シードで scroll p95・redraw 予算内（§2.3 フル再計測・12-2 ハーネス）・bootstrap 一括計算が D6 予算内 | perf 計測（値を DD に記録） |
| 8 | features.json・デモシナリオ・CHANGELOG（`wrapColumns` API）更新 | doc 確認＋e2e:showcase green |

## タスク一覧

### Phase 0: 事前精査（dd-implementer が着手時に実施）
- [ ] 📋 精査・📐 詳細化（base-layer の描画順・chunk-store 範囲クエリの拡張点・自動高の保持場所を確定）
- [ ] 🧑‍⚖️ Codex 要否 = **必須・high**
- [ ] 😈 DA調査（自動行高⇔スクロール位置の飛び・行分割キャッシュの肥大・overflow×選択ハイライトの重なり・DPR）

### Phase 1: オーバーフロー描画（TDD）
- [ ] `packages/render/src/base-layer.ts`（＋必要なら走査純関数を新設ファイルへ）: 左寄せ文字列セルの右方向延長描画・非空セル/変寄せ/クリップ境界・左外流入（D3）
- [ ] 🔬 機械検証（ユニットTDD: 走査・境界値）＋E2E（AC1〜3）
- [ ] 😈 DA批判レビュー

### Phase 2: 折り返し＋自動行高
- [ ] `wrapColumns` option（`packages/grid/src/index.ts`・R7 維持）→ base-layer 複数行描画（D4 行分割＋キャッシュ）
- [ ] 自動行高: 計算モジュール（TDD）＋トリガー配線（D5）＋手動優先（DD-012-4 連携）＋bootstrap 一括計算（D6 実測・予算判定）
- [ ] 🔬 機械検証（ユニット: トリガー網羅・手動優先・縮小）＋E2E（AC4〜6）
- [ ] 😈 DA批判レビュー
- [ ] Codexレビュー（high・Phase 1/2 束ね可）＋反映

### Phase 3: 性能・運用固定
- [ ] 🔬 機械検証（perf フル再計測: scroll p95・redraw・bootstrap 一括計算。12-2 ハーネス。値を本DDへ記録・AC7）
- [ ] `CHANGELOG.md`・`features.json`＋デモシナリオ（D8）・`test:e2e:showcase` green
- [ ] 🔬 機械検証（全体 green: `npm test`・invariants・e2e）
- [ ] 😈 DA批判レビュー
- [ ] 実機確認（ユーザー・showcase デモで Excel 風挙動）

## ログ

### 2026-07-15
- 起票。仕様確定（wrap=列単位・自動行高=Excel風・オーバーフローは表示のみ）。依存=DD-012-4 の行高 override・layout イベント

---

## DA批判レビュー記録

### Phase N DA批判レビュー

**DA観点:** （このPhaseで最も壊れやすいポイントは何か？）

| # | 発見した問題/改善点 | 重要度 | 再現手順（高/中は必須） | DA観点 | 対応 |
|---|-------------------|--------|----------------------|--------|------|
| 1 | | | | | |
