{
  "name": "@nanairo-sheet/pocd-bench",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "PoC-D 計測CLI（CellStore候補4実装の比較・決定論data-gen・ベンチ・replay計測）。製品昇格しない Node ツール（DD-006）。",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "bench:cellstore": "node --expose-gc --import tsx src/bench-cellstore.ts",
    "bench:recalc": "node --expose-gc --import tsx src/bench-recalc.ts",
    "bench:replay": "node --expose-gc --import tsx src/bench-replay.ts",
    "bench:row-insert": "node --expose-gc --import tsx src/bench-row-insert.ts"
  },
  "devDependencies": {
    "@nanairo-sheet/core": "*",
    "@nanairo-sheet/formula": "*",
    "@nanairo-sheet/types": "*",
    "@types/node": "^22.10.5",
    "tsx": "^4.19.2"
  }
}
